<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- TODO: Update the path below to point to your OpenAnima.Contracts location -->
    <!-- Or replace with a NuGet package reference when available -->
    <ProjectReference Include="..\..\src\OpenAnima.Contracts\OpenAnima.Contracts.csproj">
      <Private>false</Private>
    </ProjectReference>
  </ItemGroup>
</Project>