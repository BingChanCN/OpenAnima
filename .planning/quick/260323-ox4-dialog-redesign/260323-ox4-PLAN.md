---
phase: 260323-ox4-dialog-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/OpenAnima.Core/Components/Shared/ConfirmDialog.razor.css
  - src/OpenAnima.Core/Components/Shared/AnimaCreateDialog.razor.css
  - src/OpenAnima.Core/Components/Shared/ProviderDialog.razor.css
  - src/OpenAnima.Core/Components/Shared/ModuleDetailModal.razor.css
autonomous: true
requirements: [UI-REDESIGN-01]
must_haves:
  truths:
    - "Dialogs are perfectly centered on the screen regardless of sidebar state"
    - "Dialogs appear above the sidebar (no overlap/clipping)"
    - "Dialogs have a balanced rectangular shape (not thin strips)"
  artifacts:
    - path: "src/OpenAnima.Core/Components/Shared/ConfirmDialog.razor.css"
      provides: "Standardized modal styles"
---

<objective>
Redesign dialogs to be rectangular, centered relative to the viewport, and ensured to stay above the sidebar.
</objective>

<execution_context>
@/home/user/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@src/OpenAnima.Core/wwwroot/css/app.css
</context>

<tasks>

<task type="auto">
  <name>Standardize Dialog Positioning and Z-Index</name>
  <files>
    src/OpenAnima.Core/Components/Shared/ConfirmDialog.razor.css,
    src/OpenAnima.Core/Components/Shared/AnimaCreateDialog.razor.css,
    src/OpenAnima.Core/Components/Shared/ProviderDialog.razor.css,
    src/OpenAnima.Core/Components/Shared/ModuleDetailModal.razor.css
  </files>
  <action>
    Update all listed CSS files to ensure the following:
    - .modal-backdrop: z-index: 2000; position: fixed; inset: 0;
    - .modal-dialog: 
      - z-index: 2001; 
      - position: fixed; 
      - top: 50%; 
      - left: 50%; 
      - transform: translate(-50%, -50%);
      - margin: 0; (to prevent any offsets)
    - This ensures they are centered relative to the viewport, not the main content area, and stay above the sidebar (z-index 10).
  </action>
  <verify>
    grep "z-index: 2001" src/OpenAnima.Core/Components/Shared/*.razor.css
  </verify>
  <done>All dialogs have unified high z-index and viewport-relative centering.</done>
</task>

<task type="auto">
  <name>Redesign to Rectangular Dimensions</name>
  <files>
    src/OpenAnima.Core/Components/Shared/ConfirmDialog.razor.css,
    src/OpenAnima.Core/Components/Shared/AnimaCreateDialog.razor.css,
    src/OpenAnima.Core/Components/Shared/ProviderDialog.razor.css,
    src/OpenAnima.Core/Components/Shared/ModuleDetailModal.razor.css
  </files>
  <action>
    Adjust the widths and heights to achieve a balanced rectangular design:
    - For ConfirmDialog & AnimaCreateDialog (Simple): Set width to exactly 440px (balanced).
    - For ProviderDialog (Form-heavy): Set width to 560px.
    - For ModuleDetailModal (Content-heavy): Set width to 640px.
    - Remove 'min-width' and 'max-width' ranges in favor of fixed widths where appropriate to ensure the "rectangular" look.
    - Ensure internal padding is consistent (e.g., 1.5rem or 24px) for a modern, airy feel.
  </action>
  <verify>
    Inspect the width values in the modified CSS files.
  </verify>
  <done>Dialogs no longer look like "long strips" and have standardized rectangular widths.</done>
</task>

</tasks>

<success_criteria>
- All dialog CSS files updated with z-index 2001 and fixed centering.
- Widths standardized to 440px, 560px, or 640px depending on content.
- Visual consistency across all modal components.
</success_criteria>
