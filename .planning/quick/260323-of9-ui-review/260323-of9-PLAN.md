---
phase: ui-review
plan: 260323-of9
type: execute
wave: 1
depends_on: []
files_modified:
  - src/OpenAnima.Core/Components/Layout/MainLayout.razor.css
  - src/OpenAnima.Core/Components/Shared/EditorConfigSidebar.razor.css
  - src/OpenAnima.Core/Components/Pages/Editor.razor.css
  - src/OpenAnima.Core/wwwroot/css/app.css
autonomous: true
requirements: [UI-REVIEW-01]
must_haves:
  truths:
    - "Editor sidebar does not overlap palette area regardless of window size"
    - "Main sidebar transitions smoothly and handles text truncation correctly"
    - "UI elements have consistent spacing and refined shadow effects"
  artifacts:
    - path: "src/OpenAnima.Core/wwwroot/css/app.css"
      provides: "Global style refinements"
    - path: "src/OpenAnima.Core/Components/Shared/EditorConfigSidebar.razor.css"
      provides: "Fixed sidebar positioning"
---

<objective>
Review and fix UI design issues including spatial overlap and border overflow, while optimizing general aesthetics for a more professional feel.
</objective>

<execution_context>
@/home/user/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@src/OpenAnima.Core/wwwroot/css/app.css
@src/OpenAnima.Core/Components/Layout/MainLayout.razor.css
@src/OpenAnima.Core/Components/Shared/EditorConfigSidebar.razor.css
@src/OpenAnima.Core/Components/Pages/Editor.razor.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Editor Layout & Sidebar Overlap</name>
  <files>src/OpenAnima.Core/Components/Pages/Editor.razor.css, src/OpenAnima.Core/Components/Shared/EditorConfigSidebar.razor.css</files>
  <action>
    - Refactor `.editor-container` to use a more robust layout for the configuration sidebar.
    - Change `EditorConfigSidebar` from `position: absolute; right: 220px;` to a relative or flex-based positioning that interacts correctly with the `.palette-area`.
    - Ensure the sidebar slides out *next* to the palette rather than relying on a hardcoded offset that might cause overlap on small viewports.
    - Fix the `z-index` layering to ensure the canvas, sidebar, and palette have a clear visual hierarchy (Palette > Sidebar > Canvas).
  </action>
  <verify>
    <automated>grep -r "right: 220px" src/OpenAnima.Core/Components/Shared/EditorConfigSidebar.razor.css | wc -l | grep 0</automated>
  </verify>
  <done>Editor sidebar no longer uses fragile hardcoded offsets and respects the palette area.</done>
</task>

<task type="auto">
  <name>Task 2: Refine MainLayout & Sidebar Aesthetics</name>
  <files>src/OpenAnima.Core/Components/Layout/MainLayout.razor.css, src/OpenAnima.Core/wwwroot/css/app.css</files>
  <action>
    - Improve `.sidebar` transitions and ensure `.logo-text` and `.nav-label` fade out/in smoothly during collapse/expand.
    - Fix potential overflow issues in `.anima-list-section` by ensuring `overflow-y: auto` is correctly contained.
    - Add subtle `box-shadow` refinements to cards and sidebars in `app.css` to provide better depth perception.
    - Update `--border-color` and `--hover-bg` in `app.css` for better contrast in the dark theme.
    - Ensure buttons have refined `:active` states (slight scale down) and consistent focus rings.
  </action>
  <verify>
    <automated>grep "box-shadow" src/OpenAnima.Core/wwwroot/css/app.css</automated>
  </verify>
  <done>Main layout is visually more polished with smoother transitions and improved contrast.</done>
</task>

</tasks>

<success_criteria>
- No visual overlap between editor sidebar and palette.
- Sidebar collapse/expand is smooth with no text "jumping" or overflow.
- Improved depth and contrast across the dark theme UI.
</success_criteria>

<output>
After completion, create `.planning/quick/260323-of9-ui-review/260323-of9-SUMMARY.md`
</output>
