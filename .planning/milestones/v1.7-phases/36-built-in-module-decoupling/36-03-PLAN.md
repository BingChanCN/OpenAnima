---
phase: 36-built-in-module-decoupling
plan: 03
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - src/OpenAnima.Core/Modules/AnimaInputPortModule.cs
  - src/OpenAnima.Core/Modules/AnimaOutputPortModule.cs
  - src/OpenAnima.Core/Modules/AnimaRouteModule.cs
  - src/OpenAnima.Core/Modules/HttpRequestModule.cs
  - tests/OpenAnima.Tests/Modules/RoutingModulesTests.cs
  - tests/OpenAnima.Tests/Integration/CrossAnimaRoutingE2ETests.cs
  - tests/OpenAnima.Tests/Modules/HttpRequestModuleTests.cs
autonomous: true
requirements: [DECPL-01]

must_haves:
  truths:
    - "AnimaInputPortModule, AnimaOutputPortModule, and AnimaRouteModule use only Contracts config/context/routing types"
    - "HttpRequestModule uses Contracts config/context and the Contracts-level SSRF helper"
    - "Bulk default-config writes are replaced with explicit per-key SetConfigAsync calls where these modules seed defaults"
    - "Correlation IDs, routing event names, response completion behavior, and SSRF blocking behavior are preserved"
    - "No OpenAnima.Core using directives remain in these four module files"
  artifacts:
    - path: "src/OpenAnima.Core/Modules/AnimaRouteModule.cs"
      provides: "Contracts-only cross-Anima routing module"
      contains: "OpenAnima.Contracts.Routing"
    - path: "src/OpenAnima.Core/Modules/HttpRequestModule.cs"
      provides: "Contracts-only HTTP request module using Contracts SSRF helper"
      contains: "SsrfGuard"
    - path: "tests/OpenAnima.Tests/Modules/RoutingModulesTests.cs"
      provides: "Regression coverage for routing module constructor and behavior changes"
      contains: "AnimaRouteModule"
  key_links:
    - from: "src/OpenAnima.Core/Modules/AnimaInputPortModule.cs"
      to: "src/OpenAnima.Core/Modules/AnimaOutputPortModule.cs"
      via: "correlationId metadata passthrough"
      pattern: "correlationId"
    - from: "src/OpenAnima.Core/Modules/HttpRequestModule.cs"
      to: "src/OpenAnima.Contracts/Http/SsrfGuard.cs"
      via: "pre-request SSRF validation"
      pattern: "IsBlocked"
---

<objective>
Migrate the routing and HTTP built-in modules to Contracts-first dependencies while preserving all current behavior.

Purpose: This cohort still uses module-facing Core shims and the old HTTP helper location. After Plan 01, every dependency these modules need already exists in Contracts, so this plan is the mechanical source-file migration plus targeted regression coverage.

Output: Four built-in modules and their focused test surfaces use Contracts-only module-facing dependencies.
</objective>

<execution_context>
@/home/user/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-built-in-module-decoupling/36-CONTEXT.md
@.planning/phases/36-built-in-module-decoupling/36-RESEARCH.md
@.planning/phases/36-built-in-module-decoupling/36-01-PLAN.md

@src/OpenAnima.Core/Modules/AnimaInputPortModule.cs
@src/OpenAnima.Core/Modules/AnimaOutputPortModule.cs
@src/OpenAnima.Core/Modules/AnimaRouteModule.cs
@src/OpenAnima.Core/Modules/HttpRequestModule.cs
@tests/OpenAnima.Tests/Modules/RoutingModulesTests.cs
@tests/OpenAnima.Tests/Integration/CrossAnimaRoutingE2ETests.cs
@tests/OpenAnima.Tests/Modules/HttpRequestModuleTests.cs

<migration_note>
- The routing trio already has the needed Contracts routing types; the remaining work is swapping imports/constructor types and replacing bulk config seeding.
- `HttpRequestModule` must not remain an exception. It should consume the Contracts SSRF helper created in Plan 01.
</migration_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the routing trio to Contracts config/context/routing surfaces</name>
  <files>
    src/OpenAnima.Core/Modules/AnimaInputPortModule.cs,
    src/OpenAnima.Core/Modules/AnimaOutputPortModule.cs,
    src/OpenAnima.Core/Modules/AnimaRouteModule.cs,
    tests/OpenAnima.Tests/Modules/RoutingModulesTests.cs,
    tests/OpenAnima.Tests/Integration/CrossAnimaRoutingE2ETests.cs
  </files>
  <action>
    Update the three routing modules to use:
    - `IModuleConfig` instead of `IAnimaModuleConfigService`
    - `IModuleContext` instead of `IAnimaContext`
    - `OpenAnima.Contracts.Routing` instead of `OpenAnima.Core.Routing`
    - `ModuleMetadataRecord` from Contracts

    Replace all default-config seeding that currently uses bulk `SetConfigAsync(Dictionary<string,string>)` with explicit per-key writes. Keep the seeded keys and values exactly the same as today.

    Preserve all runtime behavior:
    - `AnimaInputPortModule` still registers the service port and republishes incoming requests with correlation metadata
    - `AnimaOutputPortModule` still completes requests by correlation ID and keeps current warning paths
    - `AnimaRouteModule` still buffers requests, awaits `RouteRequestAsync`, and publishes the same success/error payload shapes

    Update the routing tests and E2E test helpers only as needed for constructor/import changes. Prefer reusing existing stubs that already implement the Core shim interfaces, because those remain assignable through the Contracts base interfaces.
  </action>
  <verify>
    <automated>bash -lc 'dotnet test /home/user/OpenAnima/tests/OpenAnima.Tests/ -q --filter "FullyQualifiedName~RoutingModulesTests|FullyQualifiedName~CrossAnimaRoutingE2ETests"'</automated>
  </verify>
  <done>The routing module trio has no OpenAnima.Core using directives, preserves current routing behavior, and targeted routing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Move HttpRequestModule to Contracts config/context and Contracts SSRF helper</name>
  <files>
    src/OpenAnima.Core/Modules/HttpRequestModule.cs,
    tests/OpenAnima.Tests/Modules/HttpRequestModuleTests.cs
  </files>
  <action>
    Update `HttpRequestModule` to use:
    - `IModuleConfig`
    - `IModuleContext`
    - `ModuleMetadataRecord` from Contracts
    - the Contracts-owned `SsrfGuard` helper created in Plan 01

    Replace the default config initialization block with explicit per-key writes for the current `url`, `method`, `headers`, and `body` keys.

    Keep everything else intact:
    - `IHttpClientFactory` usage
    - request-body buffering
    - response body/status/error publishing
    - current SSRF-blocked and network-failure behavior

    Update `HttpRequestModuleTests` only where imports or helper namespaces changed. Do not rewrite the test scenarios.
  </action>
  <verify>
    <automated>bash -lc 'dotnet test /home/user/OpenAnima/tests/OpenAnima.Tests/ -q --filter "FullyQualifiedName~HttpRequestModuleTests"'</automated>
  </verify>
  <done>HttpRequestModule no longer imports OpenAnima.Core module-facing namespaces, still blocks unsafe URLs, and its dedicated tests pass unchanged in intent.</done>
</task>

</tasks>

<success_criteria>
- [ ] No OpenAnima.Core using directives remain in the routing and HTTP module files
- [ ] Routing modules use Contracts routing/config/context types only
- [ ] HttpRequestModule uses the Contracts SSRF helper
- [ ] Default config seeding uses per-key writes
- [ ] Routing and HTTP regression tests pass
</success_criteria>
