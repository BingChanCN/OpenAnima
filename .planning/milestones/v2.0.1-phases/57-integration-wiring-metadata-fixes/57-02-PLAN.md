---
phase: 57-integration-wiring-metadata-fixes
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/50-provider-registry/50-01-SUMMARY.md
  - .planning/phases/52-automatic-memory-recall/52-02-SUMMARY.md
autonomous: true
requirements: [PROV-08, PROV-10, MEMR-04]

must_haves:
  truths:
    - "PROV-08 appears in a plan SUMMARY requirements-completed field"
    - "PROV-10 appears in a plan SUMMARY requirements-completed field"
    - "MEMR-04 appears in a plan SUMMARY requirements-completed field"
  artifacts:
    - path: ".planning/phases/50-provider-registry/50-01-SUMMARY.md"
      provides: "requirements-completed frontmatter listing PROV-08 and PROV-10"
      contains: "requirements-completed"
    - path: ".planning/phases/52-automatic-memory-recall/52-02-SUMMARY.md"
      provides: "requirements-completed frontmatter listing MEMR-04"
      contains: "MEMR-04"
  key_links: []
---

<objective>
Patch plan SUMMARY frontmatter to close metadata tracking gaps for PROV-08, PROV-10, and MEMR-04.

Purpose: The v2.0.1 milestone audit found that these three requirements were fully implemented and verified in code but were never listed in any plan SUMMARY's `requirements-completed` frontmatter. This causes the audit tracker to report them as incomplete even though the code is done.

Output: Updated SUMMARY files with correct requirements-completed entries.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-integration-wiring-metadata-fixes/57-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PROV-08 and PROV-10 to 50-01-SUMMARY.md frontmatter</name>
  <files>.planning/phases/50-provider-registry/50-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/50-provider-registry/50-01-SUMMARY.md (current file -- has NO requirements-completed key)
  </read_first>
  <action>
The file currently has NO `requirements-completed` line in its YAML frontmatter.

Add the following line to the frontmatter block, immediately BEFORE the closing `---` delimiter (which is currently at line 49):

```yaml
requirements-completed: [PROV-08, PROV-10]
```

This line must:
- Use unquoted bracket array format (matches project convention)
- Have a single space after the colon
- Separate IDs with comma and single space
- Be on its own line, indented at column 0 (no leading spaces)

The line should be inserted after the `metrics:` block's last child (the `tests_total: 523` line at line 48) and before the `---` at line 49.

Rationale: Plan 50-01 implemented ApiKeyProtector (AES-GCM encryption, PROV-08) and ILLMProviderRegistry contract with all 4 read-only query methods (PROV-10). Both were verified working in the Phase 50 VERIFICATION but never recorded in SUMMARY metadata.
  </action>
  <verify>
    <automated>grep "requirements-completed:.*PROV-08.*PROV-10" .planning/phases/50-provider-registry/50-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - 50-01-SUMMARY.md frontmatter contains exactly: `requirements-completed: [PROV-08, PROV-10]`
    - The line is between `tests_total: 523` and `---`
    - YAML frontmatter remains valid (opening `---` at line 1, closing `---` exists)
    - No other lines in the file are modified
  </acceptance_criteria>
  <done>50-01-SUMMARY.md frontmatter includes PROV-08 and PROV-10 in requirements-completed array.</done>
</task>

<task type="auto">
  <name>Task 2: Add MEMR-04 to 52-02-SUMMARY.md frontmatter</name>
  <files>.planning/phases/52-automatic-memory-recall/52-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/52-automatic-memory-recall/52-02-SUMMARY.md (current file -- has requirements-completed: [MEMR-01, MEMR-02, MEMR-03, MEMR-05])
  </read_first>
  <action>
The file currently has on line 53:
```yaml
requirements-completed: [MEMR-01, MEMR-02, MEMR-03, MEMR-05]
```

Change this line to:
```yaml
requirements-completed: [MEMR-01, MEMR-02, MEMR-03, MEMR-04, MEMR-05]
```

Insert `MEMR-04` between `MEMR-03` and `MEMR-05` to maintain ascending ID order.

Rationale: Plan 52-02 wired MemoryRecallService into LLMModule with ranking, deduplication, truncation (500 char per node), and budget bounding (6000 char total). These are exactly the behaviors specified by MEMR-04 ("Memory injected into LLM context is ranked, deduplicated, and bounded"). The implementation was verified but MEMR-04 was omitted from the SUMMARY.
  </action>
  <verify>
    <automated>grep "requirements-completed:.*MEMR-04" .planning/phases/52-automatic-memory-recall/52-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - 52-02-SUMMARY.md line 53 reads exactly: `requirements-completed: [MEMR-01, MEMR-02, MEMR-03, MEMR-04, MEMR-05]`
    - MEMR-04 is positioned between MEMR-03 and MEMR-05 (ascending order)
    - No other lines in the file are modified
    - YAML frontmatter remains valid
  </acceptance_criteria>
  <done>52-02-SUMMARY.md frontmatter includes MEMR-04 alongside the existing MEMR-01, MEMR-02, MEMR-03, MEMR-05 entries.</done>
</task>

</tasks>

<verification>
- `grep "requirements-completed:.*PROV-08.*PROV-10" .planning/phases/50-provider-registry/50-01-SUMMARY.md` -- matches
- `grep "requirements-completed:.*MEMR-04" .planning/phases/52-automatic-memory-recall/52-02-SUMMARY.md` -- matches
- `grep -c "requirements-completed" .planning/phases/50-provider-registry/50-01-SUMMARY.md` -- returns 1 (exactly one line)
- `grep -c "requirements-completed" .planning/phases/52-automatic-memory-recall/52-02-SUMMARY.md` -- returns 1 (exactly one line)
</verification>

<success_criteria>
1. PROV-08 and PROV-10 are listed in 50-01-SUMMARY.md requirements-completed
2. MEMR-04 is listed in 52-02-SUMMARY.md requirements-completed (alongside existing entries)
3. No other file content is changed
4. YAML frontmatter is valid in both files
</success_criteria>

<output>
After completion, create `.planning/phases/57-integration-wiring-metadata-fixes/57-02-SUMMARY.md`
</output>
