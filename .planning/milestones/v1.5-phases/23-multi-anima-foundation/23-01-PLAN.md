---
phase: 23-multi-anima-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/OpenAnima.Core/Anima/AnimaDescriptor.cs
  - src/OpenAnima.Core/Anima/IAnimaRuntimeManager.cs
  - src/OpenAnima.Core/Anima/AnimaRuntimeManager.cs
  - src/OpenAnima.Core/Anima/IAnimaContext.cs
  - src/OpenAnima.Core/Anima/AnimaContext.cs
  - src/OpenAnima.Core/DependencyInjection/AnimaServiceExtensions.cs
  - src/OpenAnima.Core/Program.cs
  - tests/OpenAnima.Tests/Unit/AnimaRuntimeManagerTests.cs
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-05
  - ARCH-06
  - ANIMA-10

must_haves:
  truths:
    - "AnimaRuntimeManager can create an Anima and persist anima.json to disk"
    - "AnimaRuntimeManager can load all Animas from disk on initialization"
    - "AnimaRuntimeManager can delete an Anima and remove its directory"
    - "AnimaRuntimeManager can rename an Anima and update anima.json"
    - "AnimaRuntimeManager can clone an Anima with '(Copy)' suffix"
    - "AnimaContext tracks active Anima ID and fires change events"
    - "Services are registered as singletons in DI container"
  artifacts:
    - path: "src/OpenAnima.Core/Anima/AnimaDescriptor.cs"
      provides: "Immutable record for Anima metadata"
      contains: "record AnimaDescriptor"
    - path: "src/OpenAnima.Core/Anima/AnimaRuntimeManager.cs"
      provides: "Singleton manager for all Anima CRUD + persistence"
      exports: ["CreateAsync", "DeleteAsync", "RenameAsync", "CloneAsync", "GetAll", "InitializeAsync"]
    - path: "src/OpenAnima.Core/Anima/AnimaContext.cs"
      provides: "Active Anima selection holder with change event"
      exports: ["ActiveAnimaId", "SetActive", "ActiveAnimaChanged"]
    - path: "tests/OpenAnima.Tests/Unit/AnimaRuntimeManagerTests.cs"
      provides: "Unit tests for all CRUD operations"
      min_lines: 80
  key_links:
    - from: "src/OpenAnima.Core/DependencyInjection/AnimaServiceExtensions.cs"
      to: "src/OpenAnima.Core/Program.cs"
      via: "builder.Services.AddAnimaServices()"
      pattern: "AddAnimaServices"
    - from: "src/OpenAnima.Core/Anima/AnimaRuntimeManager.cs"
      to: "data/animas/{id}/anima.json"
      via: "System.Text.Json file I/O"
      pattern: "JsonSerializer"
---

<objective>
Build the core Anima management services: AnimaDescriptor record, AnimaRuntimeManager (singleton CRUD with filesystem persistence), AnimaContext (active selection holder), and DI registration. TDD approach — tests first for all AnimaRuntimeManager operations.

Purpose: Foundation layer that all subsequent Anima features depend on. Without this, no UI or runtime isolation is possible.
Output: Working, tested Anima CRUD services registered in DI container.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-multi-anima-foundation/23-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor should follow -->

From src/OpenAnima.Core/DependencyInjection/WiringServiceExtensions.cs:
```csharp
public static class WiringServiceExtensions
{
    public static IServiceCollection AddWiringServices(
        this IServiceCollection services,
        string? configDirectory = null)
    {
        configDirectory ??= Path.Combine(AppContext.BaseDirectory, "wiring-configs");
        Directory.CreateDirectory(configDirectory);
        services.AddSingleton<IPortRegistry, PortRegistry>();
        // ...
        return services;
    }
}
```

From src/OpenAnima.Core/Program.cs (DI registration area):
```csharp
// --- Register wiring services ---
builder.Services.AddWiringServices();
// --- Register hosted service for runtime lifecycle ---
builder.Services.AddHostedService<OpenAnimaHostedService>();
```

From tests/OpenAnima.Tests (test project uses xunit 2.9.3, net10.0, Microsoft.Extensions.Logging.Abstractions):
```xml
<PackageReference Include="xunit" Version="2.9.3" />
<PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="10.0.3" />
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AnimaDescriptor record + IAnimaRuntimeManager interface + IAnimaContext interface + AnimaRuntimeManager implementation + AnimaContext implementation + unit tests</name>
  <files>
    src/OpenAnima.Core/Anima/AnimaDescriptor.cs,
    src/OpenAnima.Core/Anima/IAnimaRuntimeManager.cs,
    src/OpenAnima.Core/Anima/AnimaRuntimeManager.cs,
    src/OpenAnima.Core/Anima/IAnimaContext.cs,
    src/OpenAnima.Core/Anima/AnimaContext.cs,
    tests/OpenAnima.Tests/Unit/AnimaRuntimeManagerTests.cs
  </files>
  <behavior>
    - CreateAsync("TestAnima") creates directory data/animas/{id}/ and writes anima.json with correct name
    - CreateAsync generates unique 8-char hex ID from Guid.NewGuid().ToString("N")[..8]
    - InitializeAsync loads all existing Animas from disk directories
    - InitializeAsync with empty directory returns empty list
    - DeleteAsync removes Anima from in-memory dictionary AND deletes directory from disk
    - DeleteAsync for non-existent ID does not throw
    - RenameAsync updates name in anima.json and in-memory dictionary
    - CloneAsync copies directory contents, appends " (Copy)" to name, generates new ID
    - GetAll returns all Animas ordered by CreatedAt
    - StateChanged event fires on Create, Delete, Rename, Clone
    - AnimaContext.SetActive sets ActiveAnimaId and fires ActiveAnimaChanged
    - AnimaContext.SetActive with same ID does not fire event
    - DisposeAsync disposes SemaphoreSlim without error
  </behavior>
  <action>
    RED phase: Write tests first in AnimaRuntimeManagerTests.cs. Use temp directory pattern (Path.GetTempPath() + Guid) matching existing ConfigurationLoaderTests. Use NullLogger from Microsoft.Extensions.Logging.Abstractions. Test class implements IAsyncDisposable for cleanup.

    GREEN phase: Implement in this order:
    1. AnimaDescriptor.cs — record with Id (string), Name (string), CreatedAt (DateTimeOffset). Use [JsonPropertyName] attributes with camelCase. Namespace: OpenAnima.Core.Anima.
    2. IAnimaRuntimeManager.cs — interface with: GetAll(), GetById(string), CreateAsync(string name, CancellationToken), DeleteAsync(string, CancellationToken), RenameAsync(string id, string newName, CancellationToken), CloneAsync(string id, CancellationToken), InitializeAsync(CancellationToken), event Action? StateChanged. Extends IAsyncDisposable.
    3. AnimaRuntimeManager.cs — singleton implementation. Constructor takes (string animasRoot, ILogger<AnimaRuntimeManager>). Uses SemaphoreSlim(1,1) for thread safety. JSON serialization with System.Text.Json (WriteIndented=true, CamelCase policy). ID generation: Guid.NewGuid().ToString("N")[..8]. Directory structure: {animasRoot}/{id}/anima.json. Delete uses Task.Run(() => Directory.Delete(dir, true)) for async. Clone copies only anima.json (not runtime state files).
    4. IAnimaContext.cs — interface with: ActiveAnimaId (string?), SetActive(string), event Action? ActiveAnimaChanged.
    5. AnimaContext.cs — singleton implementation. SetActive skips if same ID. Fires ActiveAnimaChanged on change.

    REFACTOR: Ensure all async methods properly use CancellationToken. Verify SemaphoreSlim is used around all dictionary mutations.
  </action>
  <verify>
    <automated>cd /home/user/OpenAnima && dotnet test tests/OpenAnima.Tests --filter "FullyQualifiedName~AnimaRuntimeManagerTests" --no-restore --verbosity minimal</automated>
  </verify>
  <done>All AnimaRuntimeManager and AnimaContext tests pass. CRUD operations persist to disk. Events fire correctly. IAsyncDisposable implemented.</done>
</task>

<task type="auto">
  <name>Task 2: DI registration extension + Program.cs wiring</name>
  <files>
    src/OpenAnima.Core/DependencyInjection/AnimaServiceExtensions.cs,
    src/OpenAnima.Core/Program.cs
  </files>
  <action>
    1. Create AnimaServiceExtensions.cs in DependencyInjection namespace (mirrors WiringServiceExtensions pattern):
       - Static class with AddAnimaServices(this IServiceCollection services, string? dataRoot = null) extension method
       - Default dataRoot: Path.Combine(AppContext.BaseDirectory, "data")
       - Compute animasRoot = Path.Combine(dataRoot, "animas")
       - Directory.CreateDirectory(animasRoot)
       - Register IAnimaRuntimeManager as singleton: new AnimaRuntimeManager(animasRoot, logger)
       - Register IAnimaContext as singleton: AnimaContext

    2. Update Program.cs:
       - Add `using OpenAnima.Core.Anima;` if needed
       - Add `builder.Services.AddAnimaServices();` BEFORE the AddWiringServices() call
       - This ensures Anima services are available when wiring services initialize
  </action>
  <verify>
    <automated>cd /home/user/OpenAnima && dotnet build src/OpenAnima.Core --no-restore --verbosity minimal</automated>
  </verify>
  <done>Project builds cleanly. AddAnimaServices() registered in Program.cs. AnimaRuntimeManager and AnimaContext available via DI as singletons.</done>
</task>

</tasks>

<verification>
1. `dotnet test tests/OpenAnima.Tests --filter "FullyQualifiedName~AnimaRuntimeManagerTests"` — all tests pass
2. `dotnet build src/OpenAnima.Core` — no build errors
3. Verify data/animas/ directory structure created on build output
4. Verify anima.json round-trip: create → load → verify fields match
</verification>

<success_criteria>
- AnimaRuntimeManager CRUD operations work with filesystem persistence
- AnimaContext tracks active selection with change events
- All unit tests pass (create, delete, rename, clone, load, events)
- Services registered in DI container via AddAnimaServices()
- IAsyncDisposable properly implemented on AnimaRuntimeManager
</success_criteria>

<output>
After completion, create `.planning/phases/23-multi-anima-foundation/23-01-SUMMARY.md`
</output>
