---
phase: 39-contracts-type-migration-structured-messages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/OpenAnima.Contracts/ChatMessageInput.cs
  - src/OpenAnima.Core/LLM/ILLMService.cs
  - src/OpenAnima.Core/LLM/LLMService.cs
  - src/OpenAnima.Core/LLM/TokenCounter.cs
  - src/OpenAnima.Core/Services/ChatContextManager.cs
  - tests/OpenAnima.Tests/Unit/ChatMessageInputContractsTests.cs
  - tests/OpenAnima.Tests/Integration/ModuleRuntimeInitializationTests.cs
  - tests/OpenAnima.Tests/Integration/PromptInjectionIntegrationTests.cs
  - tests/OpenAnima.Tests/Unit/ConcurrencyGuardTests.cs
  - tests/OpenAnima.Tests/Modules/ModuleTests.cs
  - tests/OpenAnima.Tests/Integration/ChatPanelModulePipelineTests.cs
  - tests/OpenAnima.Tests/Integration/ModulePipelineIntegrationTests.cs
autonomous: true
requirements: [MSG-01, MSG-03]

must_haves:
  truths:
    - "ChatMessageInput is importable from OpenAnima.Contracts namespace"
    - "All Core.LLM consumers compile without code changes beyond using alias"
    - "All 6 test files compile with added using OpenAnima.Contracts"
    - "SerializeList round-trips with DeserializeList"
    - "DeserializeList returns empty list on null/invalid input"
    - "SerializeList returns [] on null/empty input"
  artifacts:
    - path: "src/OpenAnima.Contracts/ChatMessageInput.cs"
      provides: "ChatMessageInput record + SerializeList/DeserializeList static helpers"
      contains: "namespace OpenAnima.Contracts"
    - path: "tests/OpenAnima.Tests/Unit/ChatMessageInputContractsTests.cs"
      provides: "Unit tests for MSG-01 namespace shape and MSG-03 serialization"
      min_lines: 30
  key_links:
    - from: "src/OpenAnima.Core/LLM/ILLMService.cs"
      to: "src/OpenAnima.Contracts/ChatMessageInput.cs"
      via: "using alias"
      pattern: "using ChatMessageInput = OpenAnima\\.Contracts\\.ChatMessageInput"
    - from: "tests/OpenAnima.Tests/**"
      to: "src/OpenAnima.Contracts/ChatMessageInput.cs"
      via: "using OpenAnima.Contracts"
      pattern: "using OpenAnima\\.Contracts"
---

<objective>
Move ChatMessageInput record from OpenAnima.Core.LLM to OpenAnima.Contracts and add SerializeList/DeserializeList static helper methods. Update all consumers with using aliases (Core) or using directives (tests).

Purpose: External modules need to reference ChatMessageInput without depending on Core. Serialization helpers enable JSON-on-Text transport for multi-turn messages (Phase 41 ContextModule).
Output: ChatMessageInput.cs in Contracts with helpers, all consumers updated, full test suite green.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-contracts-type-migration-structured-messages/39-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/OpenAnima.Core/LLM/ILLMService.cs (current — record to be REMOVED):
```csharp
namespace OpenAnima.Core.LLM;

public record ChatMessageInput(string Role, string Content);

public record LLMResult(bool Success, string? Content, string? Error);
public record StreamingResult(string Token, int? InputTokens, int? OutputTokens);

public interface ILLMService
{
    Task<LLMResult> CompleteAsync(IReadOnlyList<ChatMessageInput> messages, CancellationToken ct = default);
    IAsyncEnumerable<string> StreamAsync(IReadOnlyList<ChatMessageInput> messages, CancellationToken ct = default);
    IAsyncEnumerable<StreamingResult> StreamWithUsageAsync(IReadOnlyList<ChatMessageInput> messages, CancellationToken ct = default);
}
```

From src/OpenAnima.Contracts/OpenAnima.Contracts.csproj:
```xml
<TargetFramework>net8.0</TargetFramework>
<!-- System.Text.Json is BCL — no PackageReference needed -->
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ChatMessageInput in Contracts with serialization helpers + unit tests</name>
  <files>src/OpenAnima.Contracts/ChatMessageInput.cs, tests/OpenAnima.Tests/Unit/ChatMessageInputContractsTests.cs</files>
  <behavior>
    - ChatMessageInput record exists in OpenAnima.Contracts namespace with (string Role, string Content) constructor
    - SerializeList(null) returns "[]"
    - SerializeList(empty list) returns "[]"
    - SerializeList([{Role:"user", Content:"Hello"}]) returns JSON with camelCase keys
    - DeserializeList(null) returns empty list
    - DeserializeList("") returns empty list
    - DeserializeList("not json") returns empty list
    - DeserializeList(valid JSON) returns correct list with Role/Content populated
    - Round-trip: SerializeList then DeserializeList preserves all messages
  </behavior>
  <action>
    1. Create `src/OpenAnima.Contracts/ChatMessageInput.cs`:
       - `namespace OpenAnima.Contracts;` (root namespace per locked decision)
       - `public record ChatMessageInput(string Role, string Content)` with static SerializeList and DeserializeList methods
       - Private static readonly `JsonSerializerOptions` with `JsonNamingPolicy.CamelCase`
       - SerializeList: null/empty → "[]", otherwise `JsonSerializer.Serialize`
       - DeserializeList: null/whitespace → empty list, invalid JSON → empty list (try/catch, no exceptions thrown), valid → deserialized list
       - Use `System.Text.Json` (BCL, no PackageReference needed)

    2. Create `tests/OpenAnima.Tests/Unit/ChatMessageInputContractsTests.cs`:
       - Test namespace shape: `typeof(ChatMessageInput).Namespace` equals `"OpenAnima.Contracts"`
       - Test record constructor: `new ChatMessageInput("user", "hi")` has correct Role/Content
       - Test SerializeList null → "[]"
       - Test SerializeList empty → "[]"
       - Test SerializeList produces camelCase JSON
       - Test DeserializeList null → empty list
       - Test DeserializeList invalid JSON → empty list
       - Test round-trip with 3 messages (system, user, assistant)

    Write tests FIRST (RED), then create ChatMessageInput.cs (GREEN).
  </action>
  <verify>
    <automated>cd /home/user/OpenAnima && dotnet test tests/OpenAnima.Tests/OpenAnima.Tests.csproj --filter "FullyQualifiedName~ChatMessageInputContractsTests" --no-restore -v minimal</automated>
  </verify>
  <done>ChatMessageInput.cs exists in Contracts with SerializeList/DeserializeList. All unit tests pass. Record is in OpenAnima.Contracts namespace.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate consumers — remove record from ILLMService, add using aliases to Core files and using directives to test files</name>
  <files>src/OpenAnima.Core/LLM/ILLMService.cs, src/OpenAnima.Core/LLM/LLMService.cs, src/OpenAnima.Core/LLM/TokenCounter.cs, src/OpenAnima.Core/Services/ChatContextManager.cs, tests/OpenAnima.Tests/Integration/ModuleRuntimeInitializationTests.cs, tests/OpenAnima.Tests/Integration/PromptInjectionIntegrationTests.cs, tests/OpenAnima.Tests/Unit/ConcurrencyGuardTests.cs, tests/OpenAnima.Tests/Modules/ModuleTests.cs, tests/OpenAnima.Tests/Integration/ChatPanelModulePipelineTests.cs, tests/OpenAnima.Tests/Integration/ModulePipelineIntegrationTests.cs</files>
  <action>
    1. Remove `public record ChatMessageInput(string Role, string Content);` line from `src/OpenAnima.Core/LLM/ILLMService.cs`
    2. Add `using ChatMessageInput = OpenAnima.Contracts.ChatMessageInput;` to the top of these Core files (BEFORE the namespace declaration):
       - `src/OpenAnima.Core/LLM/ILLMService.cs`
       - `src/OpenAnima.Core/LLM/LLMService.cs`
       - `src/OpenAnima.Core/LLM/TokenCounter.cs`
       - `src/OpenAnima.Core/Services/ChatContextManager.cs`
    3. Add `using OpenAnima.Contracts;` to the top of these 6 test files (if not already present):
       - `tests/OpenAnima.Tests/Integration/ModuleRuntimeInitializationTests.cs`
       - `tests/OpenAnima.Tests/Integration/PromptInjectionIntegrationTests.cs`
       - `tests/OpenAnima.Tests/Unit/ConcurrencyGuardTests.cs`
       - `tests/OpenAnima.Tests/Modules/ModuleTests.cs`
       - `tests/OpenAnima.Tests/Integration/ChatPanelModulePipelineTests.cs`
       - `tests/OpenAnima.Tests/Integration/ModulePipelineIntegrationTests.cs`
    4. Verify: `dotnet build src/OpenAnima.Core/OpenAnima.Core.csproj` succeeds (zero errors)
    5. Verify: `dotnet test tests/OpenAnima.Tests/OpenAnima.Tests.csproj` — full suite green

    IMPORTANT: LLMResult and StreamingResult stay in ILLMService.cs — only ChatMessageInput moves.
    IMPORTANT: Do NOT touch Phase 36 shims (ModuleMetadataRecord, SsrfGuard) — separate concern.
    IMPORTANT: LLMModule.cs already has `using OpenAnima.Contracts;` — no change needed there.
  </action>
  <verify>
    <automated>cd /home/user/OpenAnima && dotnet build src/OpenAnima.Core/OpenAnima.Core.csproj --no-restore -v minimal && dotnet test tests/OpenAnima.Tests/OpenAnima.Tests.csproj --no-restore -v minimal</automated>
  </verify>
  <done>ChatMessageInput record removed from ILLMService.cs. All 4 Core files have using alias. All 6 test files have using directive. Full solution builds. Full test suite passes (343+ tests).</done>
</task>

</tasks>

<verification>
1. `dotnet build src/OpenAnima.Contracts/OpenAnima.Contracts.csproj` — Contracts builds with new ChatMessageInput
2. `dotnet build src/OpenAnima.Core/OpenAnima.Core.csproj` — Core builds with using aliases
3. `dotnet test tests/OpenAnima.Tests/OpenAnima.Tests.csproj` — full suite green
4. `grep -r "public record ChatMessageInput" src/` — appears ONLY in Contracts/ChatMessageInput.cs
</verification>

<success_criteria>
- ChatMessageInput record exists in OpenAnima.Contracts namespace (not a sub-namespace)
- SerializeList/DeserializeList static methods work with camelCase JSON
- ILLMService.cs no longer defines ChatMessageInput (only alias)
- All Core consumers compile via using alias
- All test consumers compile via using directive
- Full test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/39-contracts-type-migration-structured-messages/39-01-SUMMARY.md`
</output>
