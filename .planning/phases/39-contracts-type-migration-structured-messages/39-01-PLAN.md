---
phase: 39-contracts-type-migration-structured-messages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/OpenAnima.Contracts/ChatMessageInput.cs
  - src/OpenAnima.Core/LLM/ILLMService.cs
  - src/OpenAnima.Core/LLM/LLMService.cs
  - src/OpenAnima.Core/LLM/TokenCounter.cs
  - src/OpenAnima.Core/Services/ChatContextManager.cs
  - src/OpenAnima.Core/Modules/LLMModule.cs
  - src/OpenAnima.Core/Components/Shared/ChatPanel.razor
  - tests/OpenAnima.Tests/Unit/ChatMessageInputContractsTests.cs
  - tests/OpenAnima.Tests/Integration/ModulePipelineIntegrationTests.cs
  - tests/OpenAnima.Tests/Integration/ChatPanelModulePipelineTests.cs
  - tests/OpenAnima.Tests/Integration/PromptInjectionIntegrationTests.cs
  - tests/OpenAnima.Tests/Integration/ModuleRuntimeInitializationTests.cs
  - tests/OpenAnima.Tests/Unit/ConcurrencyGuardTests.cs
  - tests/OpenAnima.Tests/Modules/ModuleTests.cs
autonomous: true
requirements: [MSG-01, MSG-03]

must_haves:
  truths:
    - "ChatMessageInput type exists in OpenAnima.Contracts namespace"
    - "ChatMessageInput is no longer declared in OpenAnima.Core.LLM.ILLMService.cs"
    - "All Core and test files compile without error using the alias or direct Contracts import"
    - "SerializeList round-trips a list of messages through JSON correctly"
    - "DeserializeList returns empty list on null, empty, or invalid JSON input"
    - "SerializeList returns '[]' on null or empty input"
  artifacts:
    - path: "src/OpenAnima.Contracts/ChatMessageInput.cs"
      provides: "ChatMessageInput record with SerializeList/DeserializeList static methods"
      contains: "namespace OpenAnima.Contracts"
    - path: "tests/OpenAnima.Tests/Unit/ChatMessageInputContractsTests.cs"
      provides: "Reflection-based namespace shape tests + serialization round-trip tests"
      contains: "ChatMessageInputContractsTests"
  key_links:
    - from: "src/OpenAnima.Core/LLM/ILLMService.cs"
      to: "src/OpenAnima.Contracts/ChatMessageInput.cs"
      via: "using alias"
      pattern: "using ChatMessageInput = OpenAnima\\.Contracts\\.ChatMessageInput"
    - from: "src/OpenAnima.Core/Modules/LLMModule.cs"
      to: "src/OpenAnima.Contracts/ChatMessageInput.cs"
      via: "direct import (already imports OpenAnima.Contracts)"
      pattern: "using OpenAnima\\.Contracts"
---

<objective>
Move ChatMessageInput record from OpenAnima.Core.LLM to OpenAnima.Contracts and add SerializeList/DeserializeList static helper methods. Update all consumers with using aliases for backward compatibility.

Purpose: External modules need to reference ChatMessageInput without depending on Core. Serialization helpers enable JSON-on-Text transport for the messages port (Plan 02).
Output: ChatMessageInput.cs in Contracts with static helpers, all Core/test files compiling, unit tests for namespace shape and serialization.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-contracts-type-migration-structured-messages/39-CONTEXT.md
@.planning/phases/39-contracts-type-migration-structured-messages/39-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/OpenAnima.Core/LLM/ILLMService.cs (BEFORE migration):
```csharp
namespace OpenAnima.Core.LLM;

public record ChatMessageInput(string Role, string Content);  // THIS MOVES TO CONTRACTS

public record LLMResult(bool Success, string? Content, string? Error);  // STAYS
public record StreamingResult(string Token, int? InputTokens, int? OutputTokens);  // STAYS

public interface ILLMService
{
    Task<LLMResult> CompleteAsync(IReadOnlyList<ChatMessageInput> messages, CancellationToken ct = default);
    IAsyncEnumerable<string> StreamAsync(IReadOnlyList<ChatMessageInput> messages, CancellationToken ct = default);
    IAsyncEnumerable<StreamingResult> StreamWithUsageAsync(IReadOnlyList<ChatMessageInput> messages, CancellationToken ct = default);
}
```

Files that reference ChatMessageInput and need using alias or import update:
- src/OpenAnima.Core/LLM/ILLMService.cs (remove record, add alias)
- src/OpenAnima.Core/LLM/LLMService.cs (namespace OpenAnima.Core.LLM — implicit, needs alias)
- src/OpenAnima.Core/LLM/TokenCounter.cs (namespace OpenAnima.Core.LLM — implicit, needs alias)
- src/OpenAnima.Core/Services/ChatContextManager.cs (has `using OpenAnima.Core.LLM`)
- src/OpenAnima.Core/Modules/LLMModule.cs (already has `using OpenAnima.Contracts` — no change needed)
- src/OpenAnima.Core/Components/Shared/ChatPanel.razor (has `@using OpenAnima.Core.LLM`)
- tests/OpenAnima.Tests/Integration/ModulePipelineIntegrationTests.cs
- tests/OpenAnima.Tests/Integration/ChatPanelModulePipelineTests.cs
- tests/OpenAnima.Tests/Integration/PromptInjectionIntegrationTests.cs
- tests/OpenAnima.Tests/Integration/ModuleRuntimeInitializationTests.cs
- tests/OpenAnima.Tests/Unit/ConcurrencyGuardTests.cs
- tests/OpenAnima.Tests/Modules/ModuleTests.cs

Existing test pattern from tests/OpenAnima.Tests/Unit/ContractsApiTests.cs:
```csharp
[Trait("Category", "ContractsApi")]
public class ContractsApiTests
{
    [Fact]
    public void IModuleConfig_ExistsIn_OpenAnima_Contracts_Namespace()
    {
        var type = typeof(IModuleConfig);
        Assert.Equal("OpenAnima.Contracts", type.Namespace);
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ChatMessageInput in Contracts with serialization helpers + test scaffold</name>
  <files>
    src/OpenAnima.Contracts/ChatMessageInput.cs,
    tests/OpenAnima.Tests/Unit/ChatMessageInputContractsTests.cs
  </files>
  <behavior>
    - ChatMessageInput_ExistsIn_OpenAnima_Contracts_Namespace: typeof(ChatMessageInput).Namespace == "OpenAnima.Contracts"
    - ChatMessageInput_Is_Record_With_Role_And_Content: record has two string properties Role and Content
    - SerializeList_RoundTrips_Correctly: 3-message list serializes and deserializes back to equal list
    - SerializeList_NullInput_ReturnsEmptyArray: SerializeList(null) == "[]"
    - SerializeList_EmptyList_ReturnsEmptyArray: SerializeList(new List()) == "[]"
    - DeserializeList_NullInput_ReturnsEmptyList: DeserializeList(null).Count == 0
    - DeserializeList_EmptyString_ReturnsEmptyList: DeserializeList("").Count == 0
    - DeserializeList_InvalidJson_ReturnsEmptyList: DeserializeList("not json").Count == 0
    - DeserializeList_ValidJson_ReturnsList: DeserializeList with valid JSON returns correct messages
    - SerializeList_Uses_CamelCase: serialized JSON contains "role" and "content" (lowercase)
    - SerializeList_Has_Correct_Signature: static method accepting List<ChatMessageInput>? returning string (reflection)
    - DeserializeList_Has_Correct_Signature: static method accepting string? returning List<ChatMessageInput> (reflection)
  </behavior>
  <action>
    Write tests FIRST in ChatMessageInputContractsTests.cs following the existing ContractsApiTests.cs pattern:
    - Use `[Trait("Category", "ContractsApi")]` on the class
    - Use reflection-based tests for namespace and method signature verification
    - Use direct call tests for serialization behavior
    - Naming: `MethodName_Scenario_ExpectedResult`

    Then create `src/OpenAnima.Contracts/ChatMessageInput.cs`:
    - `namespace OpenAnima.Contracts;` (file-scoped, root namespace per user decision)
    - `public record ChatMessageInput(string Role, string Content)` with body containing:
      - `private static readonly JsonSerializerOptions _jsonOptions = new() { PropertyNamingPolicy = JsonNamingPolicy.CamelCase };`
      - `public static string SerializeList(List<ChatMessageInput>? messages)` — returns "[]" on null/empty, else JsonSerializer.Serialize
      - `public static List<ChatMessageInput> DeserializeList(string? json)` — returns empty list on null/whitespace/invalid JSON, no exceptions thrown
    - Add `using System.Text.Json;` at top (BCL, no PackageReference needed)

    Run tests — all must pass (GREEN).
  </action>
  <verify>
    <automated>dotnet test tests/OpenAnima.Tests/OpenAnima.Tests.csproj --filter "FullyQualifiedName~ChatMessageInputContractsTests" --no-restore</automated>
  </verify>
  <done>
    ChatMessageInput record exists in OpenAnima.Contracts with SerializeList/DeserializeList. All 12 behavior tests pass. Serialization uses camelCase. Edge cases (null, empty, invalid JSON) return safe defaults.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate all consumers — remove old declaration, add using aliases</name>
  <files>
    src/OpenAnima.Core/LLM/ILLMService.cs,
    src/OpenAnima.Core/LLM/LLMService.cs,
    src/OpenAnima.Core/LLM/TokenCounter.cs,
    src/OpenAnima.Core/Services/ChatContextManager.cs,
    src/OpenAnima.Core/Components/Shared/ChatPanel.razor,
    tests/OpenAnima.Tests/Integration/ModulePipelineIntegrationTests.cs,
    tests/OpenAnima.Tests/Integration/ChatPanelModulePipelineTests.cs,
    tests/OpenAnima.Tests/Integration/PromptInjectionIntegrationTests.cs,
    tests/OpenAnima.Tests/Integration/ModuleRuntimeInitializationTests.cs,
    tests/OpenAnima.Tests/Unit/ConcurrencyGuardTests.cs,
    tests/OpenAnima.Tests/Modules/ModuleTests.cs
  </files>
  <action>
    1. **ILLMService.cs**: Remove the `public record ChatMessageInput(string Role, string Content);` line. Add `using ChatMessageInput = OpenAnima.Contracts.ChatMessageInput;` at the top of the file (before the namespace declaration). LLMResult and StreamingResult stay untouched.

    2. **LLMService.cs**: Add `using ChatMessageInput = OpenAnima.Contracts.ChatMessageInput;` at the top. This file is in `namespace OpenAnima.Core.LLM` and previously resolved ChatMessageInput from the same namespace — the alias redirects it.

    3. **TokenCounter.cs**: Add `using ChatMessageInput = OpenAnima.Contracts.ChatMessageInput;` at the top. Same namespace resolution issue as LLMService.

    4. **ChatContextManager.cs**: This file has `using OpenAnima.Core.LLM;` which previously brought in ChatMessageInput. Add `using OpenAnima.Contracts;` (or the alias). Since it already has `using OpenAnima.Contracts;` on line 3, just verify ChatMessageInput resolves — it should via the Contracts namespace. If not, add the alias.

    5. **LLMModule.cs**: Already has `using OpenAnima.Contracts;` on line 4. ChatMessageInput will resolve from Contracts directly. No change needed — verify it compiles.

    6. **ChatPanel.razor**: Has `@using OpenAnima.Core.LLM` for ChatMessageInput. Add `@using OpenAnima.Contracts` if not already present. The `@using OpenAnima.Core.LLM` can stay (still needed for LLMResult if used, or other types).

    7. **Test files** (6 files): Each test file that references ChatMessageInput needs either:
       - `using OpenAnima.Contracts;` added (if not present), OR
       - `using ChatMessageInput = OpenAnima.Contracts.ChatMessageInput;` alias
       Check each file's existing imports. If it already has `using OpenAnima.Contracts;`, no change needed. If it only has `using OpenAnima.Core.LLM;`, add the Contracts using.

    After all changes, run full build to verify zero compile errors:
    `dotnet build src/OpenAnima.Core/OpenAnima.Core.csproj`

    Then run full test suite to verify no regressions.
  </action>
  <verify>
    <automated>dotnet build src/OpenAnima.Core/OpenAnima.Core.csproj && dotnet test tests/OpenAnima.Tests/OpenAnima.Tests.csproj --no-restore</automated>
  </verify>
  <done>
    ChatMessageInput declaration removed from ILLMService.cs. All 12 consumer files compile with using alias or direct Contracts import. Full test suite passes with zero regressions. No file references the old OpenAnima.Core.LLM.ChatMessageInput type directly.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build src/OpenAnima.Core/OpenAnima.Core.csproj` — zero errors
2. `dotnet build tests/OpenAnima.Tests/OpenAnima.Tests.csproj` — zero errors
3. `dotnet test tests/OpenAnima.Tests/OpenAnima.Tests.csproj --filter "FullyQualifiedName~ChatMessageInputContractsTests"` — all pass
4. `dotnet test tests/OpenAnima.Tests/OpenAnima.Tests.csproj` — full suite green (no regressions)
5. `grep -r "public record ChatMessageInput" src/OpenAnima.Core/` — zero matches (removed from Core)
6. `grep -r "public record ChatMessageInput" src/OpenAnima.Contracts/` — exactly one match (new location)
</verification>

<success_criteria>
- ChatMessageInput lives in OpenAnima.Contracts namespace (verified by reflection test)
- SerializeList/DeserializeList work correctly (verified by unit tests)
- All existing tests pass without modification to their assertions (backward compatibility)
- Zero compile errors across Core and Tests projects
</success_criteria>

<output>
After completion, create `.planning/phases/39-contracts-type-migration-structured-messages/39-01-SUMMARY.md`
</output>
