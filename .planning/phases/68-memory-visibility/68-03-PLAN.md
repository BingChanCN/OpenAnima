---
phase: 68-memory-visibility
plan: 03
type: execute
wave: 2
depends_on: ["68-01"]
files_modified:
  - src/OpenAnima.Core/Components/Shared/ChatMessage.razor
  - src/OpenAnima.Core/Components/Shared/ChatMessage.razor.css
  - src/OpenAnima.Core/Resources/SharedResources.resx
  - src/OpenAnima.Core/Resources/SharedResources.en-US.resx
  - src/OpenAnima.Core/Resources/SharedResources.zh-CN.resx
autonomous: true
requirements:
  - MEMV-01
  - MEMV-02
  - MEMV-03

must_haves:
  truths:
    - "Explicit memory tool cards reuse the existing collapsible tool-card shell and remain collapsed by default on live render and replay"
    - "Folded memory-card headers show operation label, URI, and one-line summary in the order locked by the UI spec"
    - "ToolCategory.Memory cards render with memory-specific classes and a delete-specific variant without affecting generic workspace tool cards"
    - "The sedimentation chip renders once per assistant bubble, before the generic tool-count badge, and never expands into per-node detail"
    - "All new user-facing copy comes from SharedResources resource keys rather than hard-coded strings in Razor"
  artifacts:
    - path: "src/OpenAnima.Core/Components/Shared/ChatMessage.razor"
      provides: "memory-card header rendering and badge-row chip placement"
      contains: "sedimentation-chip"
    - path: "src/OpenAnima.Core/Components/Shared/ChatMessage.razor.css"
      provides: "ToolCategory.Memory visual treatment and chip styling"
      contains: ".tool-card-memory"
    - path: "src/OpenAnima.Core/Resources/SharedResources.resx"
      provides: "localized titles and sedimentation chip copy"
      contains: "Chat.MemoryCreateTitle"
  key_links:
    - from: "src/OpenAnima.Core/Components/Shared/ChatMessage.razor"
      to: "src/OpenAnima.Core/Services/ChatSessionState.cs"
      via: "rendering uses the persisted category/URI/folded-summary/sedimentation-summary fields"
      pattern: "ToolCategory.Memory"
    - from: "src/OpenAnima.Core/Components/Shared/ChatMessage.razor"
      to: "src/OpenAnima.Core/Resources/SharedResources.resx"
      via: "operation labels and chip copy must come from resource keys"
      pattern: "Chat.MemoryCreateTitle"
---

<objective>
Render the visible Phase 68 UI contract inside the existing assistant-message component.

Purpose: Once the data contract and live event wiring exist, this plan makes the feature visible and distinct in the chat UI without introducing a second card model. It adds the memory-card header treatment, summary chip placement, and localized copy required by the context and UI spec.
Output: Memory-specific card rendering in `ChatMessage.razor`, matching scoped CSS in `ChatMessage.razor.css`, and resource keys for titles and sedimentation chip copy.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-memory-visibility/68-CONTEXT.md
@.planning/phases/68-memory-visibility/68-RESEARCH.md
@.planning/phases/68-memory-visibility/68-UI-SPEC.md

<interfaces>
From src/OpenAnima.Core/Components/Shared/ChatMessage.razor:
```razor
@if (ToolCalls.Count > 0)
{
    <div class="tool-cards">
        @foreach (var tool in ToolCalls)
        {
            <div class="tool-card @(tool.IsExpanded ? "expanded" : "")">
                <button class="tool-card-header" @onclick="() => tool.IsExpanded = !tool.IsExpanded">
                    <span class="tool-status-icon @GetStatusClass(tool.Status)">
                        @GetStatusIcon(tool.Status)
                    </span>
                    <span class="tool-name">@tool.ToolName</span>
                    <span class="tool-expand-chevron">@(tool.IsExpanded ? "\u25B2" : "\u25BC")</span>
                </button>
```

From src/OpenAnima.Core/Components/Shared/ChatMessage.razor.css:
```css
.tool-card {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 8px;
    overflow: hidden;
}
```

From .planning/phases/68-memory-visibility/68-UI-SPEC.md:
- Memory card folded header order is fixed: status icon, memory icon, operation label, target URI, one-line summary, chevron
- Sedimentation summary chip appears below assistant markdown in the badge row before the generic tool-count badge
- Memory cards use a subtle memory-specific accent treatment; delete cards use destructive badge treatment only
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render memory-card headers and the sedimentation chip in ChatMessage</name>
  <files>
    src/OpenAnima.Core/Components/Shared/ChatMessage.razor
  </files>
  <read_first>
    src/OpenAnima.Core/Components/Shared/ChatMessage.razor,
    src/OpenAnima.Core/Components/Shared/ChatMessage.razor.css,
    src/OpenAnima.Core/Services/ChatSessionState.cs,
    src/OpenAnima.Core/Resources/SharedResources.resx,
    .planning/phases/68-memory-visibility/68-UI-SPEC.md
  </read_first>
  <action>
    1. In `src/OpenAnima.Core/Components/Shared/ChatMessage.razor`, keep the single `@foreach (var tool in ToolCalls)` loop and the existing collapse interaction.
    2. Replace the header body so memory cards render this order:
       - status icon
       - memory-category icon span when `tool.Category == ToolCategory.Memory` with `aria-hidden="true"`
       - localized operation title using `tool.ToolName`:
         - `memory_create` -> `L["Chat.MemoryCreateTitle"]`
         - `memory_update` -> `L["Chat.MemoryUpdateTitle"]`
         - `memory_delete` -> `L["Chat.MemoryDeleteTitle"]`
       - URI span/pill using `tool.TargetUri`
       - folded summary span using `tool.FoldedSummary` when not empty
       - existing chevron
    3. Keep generic workspace tools on the old title path (`tool.ToolName`) when `tool.Category != ToolCategory.Memory`.
    4. Add a badge-row container rendered when `!IsStreaming` and either:
       - `ToolCalls.Count > 0`, or
       - `SedimentationSummary?.Count > 0`
    5. Inside that badge row:
       - render the sedimentation chip first when `SedimentationSummary?.Count > 0`
       - use singular/plural resource keys based on count
       - render the existing generic tool-count badge after the chip when `ToolCalls.Count > 0`
    6. In the expanded body path for successful delete cards, append the localized note `L["Chat.MemoryDeleteRecoverable"]` below the result summary so the soft-delete semantics remain visible.
  </action>
  <verify>
    <automated>cd /home/user/OpenAnima && dotnet build src/OpenAnima.Core/OpenAnima.Core.csproj --no-restore -v q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/OpenAnima.Core/Components/Shared/ChatMessage.razor contains `tool.Category == ToolCategory.Memory`
    - src/OpenAnima.Core/Components/Shared/ChatMessage.razor contains `L["Chat.MemoryCreateTitle"]`
    - src/OpenAnima.Core/Components/Shared/ChatMessage.razor contains `L["Chat.MemoryUpdateTitle"]`
    - src/OpenAnima.Core/Components/Shared/ChatMessage.razor contains `L["Chat.MemoryDeleteTitle"]`
    - src/OpenAnima.Core/Components/Shared/ChatMessage.razor contains `tool.TargetUri`
    - src/OpenAnima.Core/Components/Shared/ChatMessage.razor contains `tool.FoldedSummary`
    - src/OpenAnima.Core/Components/Shared/ChatMessage.razor contains `SedimentationSummary?.Count`
    - src/OpenAnima.Core/Components/Shared/ChatMessage.razor contains `Chat.MemoryDeleteRecoverable`
  </acceptance_criteria>
  <done>ChatMessage renders explicit memory operations and the sedimentation count chip inside the existing assistant-message shell.</done>
</task>

<task type="auto">
  <name>Task 2: Add memory-specific CSS and localized resource keys</name>
  <files>
    src/OpenAnima.Core/Components/Shared/ChatMessage.razor.css,
    src/OpenAnima.Core/Resources/SharedResources.resx,
    src/OpenAnima.Core/Resources/SharedResources.en-US.resx,
    src/OpenAnima.Core/Resources/SharedResources.zh-CN.resx
  </files>
  <read_first>
    src/OpenAnima.Core/Components/Shared/ChatMessage.razor.css,
    src/OpenAnima.Core/Resources/SharedResources.resx,
    src/OpenAnima.Core/Resources/SharedResources.en-US.resx,
    src/OpenAnima.Core/Resources/SharedResources.zh-CN.resx,
    .planning/phases/68-memory-visibility/68-UI-SPEC.md
  </read_first>
  <action>
    1. In `src/OpenAnima.Core/Components/Shared/ChatMessage.razor.css`, add concrete classes for the new visual contract:
       - `.tool-card-memory` with a subtle accent border/stripe and memory-specific surface treatment
       - `.tool-card-delete` with destructive title/badge text treatment without turning the whole card red
       - `.memory-card-icon`
       - `.tool-uri-pill`
       - `.tool-summary`
       - `.message-badges`
       - `.sedimentation-chip`
    2. Keep the memory treatment intentionally moderate:
       - accent stripe, icon/title accent, subtle background wash
       - chip quieter than explicit cards
       - generic tool cards unchanged
    3. In all three resource files, add these exact keys:
       - `Chat.MemoryCreateTitle`
       - `Chat.MemoryUpdateTitle`
       - `Chat.MemoryDeleteTitle`
       - `Chat.SedimentationChipSingular`
       - `Chat.SedimentationChipPlural`
       - `Chat.MemoryDeleteRecoverable`
    4. Use these English values in `SharedResources.resx` and `SharedResources.en-US.resx`:
       - `Create memory`
       - `Update memory`
       - `Delete memory`
       - `{0} memory sedimented`
       - `{0} memories sedimented`
       - `Soft deleted. Recoverable from Memory page.`
    5. Add corresponding Chinese translations in `SharedResources.zh-CN.resx` instead of copying the English strings.
  </action>
  <verify>
    <automated>cd /home/user/OpenAnima && dotnet build src/OpenAnima.Core/OpenAnima.Core.csproj --no-restore -v q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/OpenAnima.Core/Components/Shared/ChatMessage.razor.css contains `.tool-card-memory`
    - src/OpenAnima.Core/Components/Shared/ChatMessage.razor.css contains `.tool-card-delete`
    - src/OpenAnima.Core/Components/Shared/ChatMessage.razor.css contains `.tool-uri-pill`
    - src/OpenAnima.Core/Components/Shared/ChatMessage.razor.css contains `.sedimentation-chip`
    - src/OpenAnima.Core/Resources/SharedResources.resx contains `Chat.MemoryCreateTitle`
    - src/OpenAnima.Core/Resources/SharedResources.resx contains `Chat.SedimentationChipPlural`
    - src/OpenAnima.Core/Resources/SharedResources.zh-CN.resx contains `Chat.MemoryDeleteRecoverable`
  </acceptance_criteria>
  <done>The visible memory-card style, chip placement, and localized copy required by the UI contract are in place.</done>
</task>

</tasks>
