---
phase: 68-memory-visibility
plan: 02
type: execute
wave: 2
depends_on: ["68-01"]
files_modified:
  - src/OpenAnima.Core/Services/ChatMemoryVisibilityProjector.cs
  - src/OpenAnima.Core/Components/Shared/ChatPanel.razor
  - src/OpenAnima.Core/Memory/SedimentationService.cs
  - tests/OpenAnima.Tests/Unit/ChatMemoryVisibilityProjectorTests.cs
  - tests/OpenAnima.Tests/Unit/SedimentationServiceTests.cs
autonomous: true
requirements:
  - MEMV-01
  - MEMV-02

must_haves:
  truths:
    - "memory_create, memory_update, and memory_delete are classified as ToolCategory.Memory as soon as the running tool card is created"
    - "memory_list remains on the generic tool-card path in this phase"
    - "Memory.operation hydrates the latest matching memory card with its normalized URI and one-line folded summary"
    - "Background sedimentation publishes one aggregate SedimentationCompletedPayload only when written count is greater than zero"
    - "ChatPanel attaches the sedimentation summary to the originating assistant bubble and persists it whether the event arrives before or after the assistant row id is known"
  artifacts:
    - path: "src/OpenAnima.Core/Services/ChatMemoryVisibilityProjector.cs"
      provides: "centralized mapping logic for memory-card classification and folded-summary hydration"
      contains: "class ChatMemoryVisibilityProjector"
    - path: "src/OpenAnima.Core/Components/Shared/ChatPanel.razor"
      provides: "Memory.operation and sedimentation event subscriptions plus persistence race handling"
      contains: "Memory.sedimentation.completed"
    - path: "src/OpenAnima.Core/Memory/SedimentationService.cs"
      provides: "aggregate count-only sedimentation event publication"
      contains: "SedimentationCompletedPayload"
  key_links:
    - from: "src/OpenAnima.Core/Memory/SedimentationService.cs"
      to: "src/OpenAnima.Core/Components/Shared/ChatPanel.razor"
      via: "count-only sedimentation event drives the summary chip attachment path"
      pattern: "Memory.sedimentation.completed"
    - from: "src/OpenAnima.Core/Components/Shared/ChatPanel.razor"
      to: "src/OpenAnima.Core/ChatPersistence/ChatHistoryService.cs"
      via: "assistant bubble updates must be persisted after the initial insert"
      pattern: "UpdateAssistantVisibilityAsync"
    - from: "src/OpenAnima.Core/Components/Shared/ChatPanel.razor"
      to: "src/OpenAnima.Core/Services/ChatMemoryVisibilityProjector.cs"
      via: "tool-start and memory-operation mapping stays testable and out of the Razor event handlers"
      pattern: "ChatMemoryVisibilityProjector"
---

<objective>
Wire the live Phase 68 memory-visibility flow in the chat runtime.

Purpose: The existing chat tool-card shell is already correct. This plan adds the missing live event correlation path so explicit memory tools become memory cards, background sedimentation becomes one aggregate summary chip, and both updates persist onto the correct assistant message.
Output: A projector/helper for deterministic memory-visibility mapping, aggregate sedimentation event publication, and ChatPanel event handlers that update both live in-memory messages and the persisted assistant row.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-memory-visibility/68-CONTEXT.md
@.planning/phases/68-memory-visibility/68-RESEARCH.md
@.planning/phases/68-memory-visibility/68-01-PLAN.md

<interfaces>
From src/OpenAnima.Core/Components/Shared/ChatPanel.razor:
```csharp
_toolCallStartedSubscription = EventBus.Subscribe<ToolCallStartedPayload>(
    "LLMModule.tool_call.started", HandleToolCallStartedAsync);
_toolCallCompletedSubscription = EventBus.Subscribe<ToolCallCompletedPayload>(
    "LLMModule.tool_call.completed", HandleToolCallCompletedAsync);
```

From src/OpenAnima.Core/Tools/MemoryCreateTool.cs / MemoryUpdateTool.cs / MemoryDeleteTool.cs:
```csharp
await _eventBus.PublishAsync(new ModuleEvent<MemoryOperationPayload>
{
    EventName = "Memory.operation",
    SourceModuleId = "MemoryTools",
    Payload = new MemoryOperationPayload("create" | "update" | "delete", animaId, uriOrPath, contentOrNull, null, true)
}, ct);
```

From src/OpenAnima.Core/Memory/SedimentationService.cs:
```csharp
var outputSummary = $"{writtenUris.Count} nodes sedimented: {string.Join(", ", writtenUris)}";
await (_stepRecorder?.RecordStepCompleteAsync(stepId, "Sedimentation", outputSummary, ct) ?? Task.CompletedTask);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create a projector/helper that classifies memory tools and hydrates folded metadata</name>
  <files>
    src/OpenAnima.Core/Services/ChatMemoryVisibilityProjector.cs,
    tests/OpenAnima.Tests/Unit/ChatMemoryVisibilityProjectorTests.cs
  </files>
  <read_first>
    src/OpenAnima.Core/Services/ChatSessionState.cs,
    src/OpenAnima.Core/Events/ChatEvents.cs,
    src/OpenAnima.Core/Tools/MemoryCreateTool.cs,
    src/OpenAnima.Core/Tools/MemoryUpdateTool.cs,
    src/OpenAnima.Core/Tools/MemoryDeleteTool.cs,
    src/OpenAnima.Core/Tools/MemoryListTool.cs,
    .planning/phases/68-memory-visibility/68-CONTEXT.md,
    .planning/phases/68-memory-visibility/68-UI-SPEC.md
  </read_first>
  <behavior>
    - `memory_create`, `memory_update`, and `memory_delete` start as `ToolCategory.Memory`
    - `memory_list` stays `ToolCategory.Generic`
    - create uses `path` as the normalized URI source; update/delete use `uri`
    - folded summary is clamped to one persisted line with a hard cap of 80 characters before ellipsis
    - delete cards do not fabricate folded summary text
    - assistant-target selection prefers the streaming assistant bubble and falls back to the latest completed assistant bubble
  </behavior>
  <action>
    1. Create `src/OpenAnima.Core/Services/ChatMemoryVisibilityProjector.cs` with a small testable API. The class may be static or instance-based, but it must expose concrete methods with names equivalent to:
       - `CreateToolCallInfo(string toolName, IReadOnlyDictionary<string, string> parameters)`
       - `ApplyMemoryOperation(ChatSessionMessage message, MemoryOperationPayload payload)`
       - `ApplySedimentationSummary(ChatSessionMessage message, int writtenCount)`
       - `FindAssistantTarget(IReadOnlyList<ChatSessionMessage> messages)`
    2. `CreateToolCallInfo(...)` must:
       - return a `ToolCallInfo` with `Category = ToolCategory.Memory` only for `memory_create`, `memory_update`, and `memory_delete`
       - set `TargetUri` from `path` for `memory_create`
       - set `TargetUri` from `uri` for `memory_update` and `memory_delete`
       - leave `memory_list` as `ToolCategory.Generic`
    3. `ApplyMemoryOperation(...)` must:
       - locate the latest matching tool card for the given operation on the current assistant message
       - hydrate `TargetUri` from `payload.Uri`
       - set `FoldedSummary` from `payload.Content` for create/update with an 80-character clamp and `...` suffix when truncated
       - leave `FoldedSummary` null/empty for delete
    4. `ApplySedimentationSummary(...)` must:
       - set exactly one `message.SedimentationSummary = new SedimentationSummaryInfo { Count = writtenCount }`
       - overwrite any previous value instead of appending a second summary object
    5. Create `tests/OpenAnima.Tests/Unit/ChatMemoryVisibilityProjectorTests.cs` covering:
       - memory_create classification + `path` normalization
       - memory_update classification + `uri` normalization
       - memory_delete has null folded summary
       - memory_list stays generic
       - create/update summary text truncates at 80 characters with ellipsis
       - sedimentation summary count replaces rather than duplicates
       - assistant-target selection prefers a streaming assistant message and otherwise falls back to the latest non-streaming assistant message
  </action>
  <verify>
    <automated>cd /home/user/OpenAnima && dotnet test tests/OpenAnima.Tests --filter "ChatMemoryVisibilityProjector" --no-restore -v q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/OpenAnima.Core/Services/ChatMemoryVisibilityProjector.cs contains `class ChatMemoryVisibilityProjector`
    - src/OpenAnima.Core/Services/ChatMemoryVisibilityProjector.cs contains `memory_create` and `memory_update` and `memory_delete`
    - src/OpenAnima.Core/Services/ChatMemoryVisibilityProjector.cs contains `ToolCategory.Memory`
    - src/OpenAnima.Core/Services/ChatMemoryVisibilityProjector.cs contains `path` and `uri`
    - src/OpenAnima.Core/Services/ChatMemoryVisibilityProjector.cs contains `80`
    - src/OpenAnima.Core/Services/ChatMemoryVisibilityProjector.cs contains `FindAssistantTarget`
    - tests/OpenAnima.Tests/Unit/ChatMemoryVisibilityProjectorTests.cs contains `memory_list`
  </acceptance_criteria>
  <done>Memory-visibility mapping logic is centralized, deterministic, and unit-tested outside of the ChatPanel Razor event handlers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish the aggregate sedimentation event and wire ChatPanel to update/persist the correct assistant bubble</name>
  <files>
    src/OpenAnima.Core/Memory/SedimentationService.cs,
    src/OpenAnima.Core/Components/Shared/ChatPanel.razor,
    tests/OpenAnima.Tests/Unit/SedimentationServiceTests.cs
  </files>
  <read_first>
    src/OpenAnima.Core/Memory/SedimentationService.cs,
    src/OpenAnima.Core/Modules/LLMModule.cs,
    src/OpenAnima.Core/Components/Shared/ChatPanel.razor,
    src/OpenAnima.Core/ChatPersistence/ChatHistoryService.cs,
    src/OpenAnima.Core/Services/ChatMemoryVisibilityProjector.cs,
    src/OpenAnima.Core/Events/ChatEvents.cs,
    tests/OpenAnima.Tests/Unit/SedimentationServiceTests.cs,
    .planning/phases/68-memory-visibility/68-RESEARCH.md
  </read_first>
  <behavior>
    - Sedimentation publishes a count-only event when `writtenUris.Count > 0`
    - ChatPanel subscribes to `Memory.operation` and `Memory.sedimentation.completed`
    - ChatPanel replaces the inline `new ToolCallInfo { ... }` start-path with projector-generated tool-call state
    - The assistant message stores the returned persistence id after `StoreMessageAsync(...)`
    - If sedimentation arrives before that id exists, the summary stays in-memory and is persisted once the id becomes available
    - If sedimentation arrives after the id exists, ChatPanel immediately calls `UpdateAssistantVisibilityAsync(...)`
  </behavior>
  <action>
    1. In `src/OpenAnima.Core/Memory/SedimentationService.cs`:
       - Add an optional `IEventBus? eventBus = null` constructor dependency and backing field.
       - After writes complete and only when `writtenUris.Count > 0`, publish:
       ```csharp
       await _eventBus.PublishAsync(new ModuleEvent<SedimentationCompletedPayload>
       {
           EventName = "Memory.sedimentation.completed",
           SourceModuleId = "SedimentationService",
           Payload = new SedimentationCompletedPayload(animaId, writtenUris.Count)
       }, ct);
       ```
       - Keep the event count-only. Do not publish URI-level detail to the chat surface.

    2. In `src/OpenAnima.Core/Components/Shared/ChatPanel.razor`:
       - Add subscription fields for `MemoryOperationPayload` and `SedimentationCompletedPayload`.
       - Subscribe in `OnInitialized()`:
         - `EventBus.Subscribe<MemoryOperationPayload>("Memory.operation", HandleMemoryOperationAsync)`
         - `EventBus.Subscribe<SedimentationCompletedPayload>("Memory.sedimentation.completed", HandleSedimentationCompletedAsync)`
       - Replace the current inline `new ToolCallInfo { ... }` in `HandleToolCallStartedAsync(...)` with `ChatMemoryVisibilityProjector.CreateToolCallInfo(...)`.
       - Add `HandleMemoryOperationAsync(...)` to call the projector and then `StateHasChanged`.
       - Add `HandleSedimentationCompletedAsync(...)` that:
         - finds the current assistant message via `ChatMemoryVisibilityProjector.FindAssistantTarget(...)`
         - attaches exactly one summary count to that message
         - calls `_chatHistoryService.UpdateAssistantVisibilityAsync(...)` immediately when `PersistenceId` is already set
       - In `GenerateAssistantResponseAsync(...)`, capture the return value of `_chatHistoryService.StoreMessageAsync(...)` into `assistantMessage.PersistenceId`.
       - After storing the assistant row, if `assistantMessage.SedimentationSummary` is already populated, immediately call `_chatHistoryService.UpdateAssistantVisibilityAsync(...)` so the race path persists correctly.
       - Dispose the new subscriptions in `DisposeAsync()`.

    3. In `tests/OpenAnima.Tests/Unit/SedimentationServiceTests.cs`:
       - Add a test that when sedimentation writes 2 items, a `SedimentationCompletedPayload` with `WrittenCount == 2` is published once.
       - Add a test that when sedimentation writes 0 items, no sedimentation-complete event is published.
  </action>
  <verify>
    <automated>cd /home/user/OpenAnima && dotnet test tests/OpenAnima.Tests --filter "SedimentationService|ChatMemoryVisibilityProjector" --no-restore -v q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/OpenAnima.Core/Memory/SedimentationService.cs contains `EventName = "Memory.sedimentation.completed"`
    - src/OpenAnima.Core/Memory/SedimentationService.cs contains `SedimentationCompletedPayload`
    - src/OpenAnima.Core/Components/Shared/ChatPanel.razor contains `Subscribe<MemoryOperationPayload>("Memory.operation"`
    - src/OpenAnima.Core/Components/Shared/ChatPanel.razor contains `Subscribe<SedimentationCompletedPayload>("Memory.sedimentation.completed"`
    - src/OpenAnima.Core/Components/Shared/ChatPanel.razor contains `ChatMemoryVisibilityProjector.CreateToolCallInfo`
    - src/OpenAnima.Core/Components/Shared/ChatPanel.razor contains `assistantMessage.PersistenceId`
    - src/OpenAnima.Core/Components/Shared/ChatPanel.razor contains `UpdateAssistantVisibilityAsync`
  </acceptance_criteria>
  <done>Live chat execution updates the correct assistant bubble for explicit memory tools and background sedimentation, and the result persists through the race between response completion and background writes.</done>
</task>

</tasks>
