---
phase: 36-built-in-module-decoupling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - src/OpenAnima.Contracts/ModuleMetadataRecord.cs
  - src/OpenAnima.Contracts/Http/SsrfGuard.cs
  - src/OpenAnima.Core/Modules/ModuleMetadataRecord.cs
  - src/OpenAnima.Core/Http/SsrfGuard.cs
  - tests/OpenAnima.Tests/Unit/ContractsApiTests.cs
  - tests/OpenAnima.Tests/Unit/SsrfGuardTests.cs
autonomous: true
requirements: [DECPL-01, DECPL-05]

must_haves:
  truths:
    - "Phase 36 docs use an authoritative inventory: 12 active built-in module types, with FormatDetector and ModuleMetadataRecord treated as helper scope rather than counted modules"
    - "Phase 36 docs explicitly record the allowed LLMModule Core.LLM exception instead of promising absolute zero-Core purity"
    - "The historical candidates behind the old 14-count are explicitly dispositioned: helper-file miscount, unshipped BUILTIN-11/12 backlog items, and removed demo modules are all called out as not part of the live active inventory"
    - "ModuleMetadataRecord exists in OpenAnima.Contracts and is the shared metadata pattern for built-in and generated modules"
    - "SsrfGuard exists in Contracts (or an equivalent minimal Contracts-level HTTP helper surface) so HttpRequestModule no longer needs a Core-only helper later"
    - "Temporary Core shims keep the codebase building unchanged until later migration plans switch consumers to the Contracts types directly"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Normalized Phase 36 goal, success criteria, and discrepancy disposition note"
      contains: "12 active built-in modules"
    - path: "src/OpenAnima.Contracts/ModuleMetadataRecord.cs"
      provides: "Shared metadata record in Contracts"
      contains: "record ModuleMetadataRecord"
    - path: "src/OpenAnima.Contracts/Http/SsrfGuard.cs"
      provides: "Contracts-level SSRF helper"
      contains: "static class SsrfGuard"
    - path: "src/OpenAnima.Core/Modules/ModuleMetadataRecord.cs"
      provides: "Temporary Core compatibility shim"
      contains: "OpenAnima.Contracts.ModuleMetadataRecord"
  key_links:
    - from: "src/OpenAnima.Core/Modules/ModuleMetadataRecord.cs"
      to: "src/OpenAnima.Contracts/ModuleMetadataRecord.cs"
      via: "compatibility alias or forwarding shim"
      pattern: "OpenAnima\\.Contracts\\.ModuleMetadataRecord"
    - from: "src/OpenAnima.Core/Http/SsrfGuard.cs"
      to: "src/OpenAnima.Contracts/Http/SsrfGuard.cs"
      via: "compatibility alias or forwarding shim"
      pattern: "OpenAnima\\.Contracts\\.Http\\.SsrfGuard"
---

<objective>
Normalize the real Phase 36 migration target and move the shared helper types that block Contracts-first built-in modules.

Purpose: Execution cannot be verified honestly until the repo stops claiming "14 built-in modules" when the runtime actually registers 12 module classes plus two helper files. This plan establishes the authoritative inventory, records the explicit LLM exception, and promotes `ModuleMetadataRecord` plus the HTTP SSRF helper into Contracts with temporary Core shims so later plans can migrate module cohorts safely.

Output: Canonical docs aligned on the real inventory, historical disposition, and exception policy; `ModuleMetadataRecord` and `SsrfGuard` available from Contracts; existing callers still compile via transitional Core shims.
</objective>

<execution_context>
@/home/user/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/36-built-in-module-decoupling/36-RESEARCH.md
@.planning/milestones/v1.5-REQUIREMENTS.md

@src/OpenAnima.Core/DependencyInjection/WiringServiceExtensions.cs
@src/OpenAnima.Core/Hosting/WiringInitializationService.cs
@src/OpenAnima.Core/Modules/ModuleMetadataRecord.cs
@src/OpenAnima.Core/Http/SsrfGuard.cs
@src/OpenAnima.Contracts/IModuleMetadata.cs
@tests/OpenAnima.Tests/Unit/ContractsApiTests.cs
@tests/OpenAnima.Tests/Unit/SsrfGuardTests.cs

<inventory_note>
- The live runtime registers 12 built-in module classes in both `WiringServiceExtensions` and `WiringInitializationService`.
- The misleading "14" count comes from helper files in `src/OpenAnima.Core/Modules/` plus stale planning language, not from two missing module implementations.
- User decision allows `LLMModule` to remain the one documented Core.LLM exception in Phase 36.
</inventory_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize the authoritative built-in inventory and exception wording across active docs</name>
  <files>
    .planning/PROJECT.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md
  </files>
  <action>
    Re-audit the Phase 36 target from code before editing text: count the module registrations in `WiringServiceExtensions` and `WiringInitializationService`, then compare that active set against `src/OpenAnima.Core/Modules/`.

    Update the listed planning/docs files so normative wording becomes:
    - `12 active built-in modules` for the runtime-counted module classes
    - `FormatDetector` and `ModuleMetadataRecord` described as helper/support types that remain in Phase 36 scope but are NOT counted as built-in module instances
    - `LLMModule` explicitly called out as the one allowed `OpenAnima.Core.LLM` exception unless/until a later phase promotes the LLM service surface

    Requirements for the edits:
    - Preserve requirement IDs `DECPL-01` through `DECPL-05`; do not renumber or delete them
    - Rewrite Phase 36 goal/success criteria language so it matches the user-approved exception policy instead of promising impossible absolute purity
    - Add an explicit discrepancy-disposition note in the canonical docs that names the historical candidates and their disposition:
      - `FormatDetector` and `ModuleMetadataRecord` are helper/support files, not active built-in modules
      - `BUILTIN-11` and `BUILTIN-12` remained unshipped backlog items from v1.5, not missing live implementations
      - removed demo nodes (`TextInput`, `LLMProcessor`, `TextOutput`, `TriggerButton`) are historical and not part of the active inventory
    - Do not "fix" this by inventing two missing built-in modules; document the real source of the discrepancy directly in the canonical Phase 36 wording
  </action>
  <verify>
    <automated>bash -lc 'files=".planning/PROJECT.md .planning/ROADMAP.md .planning/REQUIREMENTS.md"; rg -n "12 active built-in modules|LLMModule.*exception|BUILTIN-11|BUILTIN-12|FormatDetector|ModuleMetadataRecord|TextInput|LLMProcessor|TextOutput|TriggerButton" $files >/dev/null && test -z "$(rg -n "14 built-in modules|all 14 built-in modules|Migrate all 14 built-in modules" $files)"'</automated>
  </verify>
  <done>Canonical project docs no longer describe 14 active built-in modules. They describe the real 12-module runtime inventory, explicitly disposition the historical candidates behind the old count, and record the single LLMModule exception.</done>
</task>

<task type="auto">
  <name>Task 2: Move shared metadata and SSRF helpers to Contracts with temporary Core compatibility shims</name>
  <files>
    src/OpenAnima.Contracts/ModuleMetadataRecord.cs,
    src/OpenAnima.Contracts/Http/SsrfGuard.cs,
    src/OpenAnima.Core/Modules/ModuleMetadataRecord.cs,
    src/OpenAnima.Core/Http/SsrfGuard.cs,
    tests/OpenAnima.Tests/Unit/ContractsApiTests.cs,
    tests/OpenAnima.Tests/Unit/SsrfGuardTests.cs
  </files>
  <action>
    Add `ModuleMetadataRecord` to `OpenAnima.Contracts` as the shared concrete implementation of `IModuleMetadata`. Match the existing record surface exactly and keep XML docs concise.

    Add `SsrfGuard` to `OpenAnima.Contracts.Http` by moving the existing static helper surface unchanged unless a tiny namespace-only adjustment is required. Do NOT redesign it into a broader network abstraction in this plan.

    Replace the current Core helper files with compatibility shims so the rest of the repo still compiles before later plans touch consumers:
    - `src/OpenAnima.Core/Modules/ModuleMetadataRecord.cs`: global alias, forwarding record, or equivalent minimal shim pointing at the Contracts type
    - `src/OpenAnima.Core/Http/SsrfGuard.cs`: global alias or forwarding static class to the Contracts helper

    Update tests so Contracts ownership is explicit:
    - `ContractsApiTests` should assert `ModuleMetadataRecord` lives in the Contracts assembly/namespace
    - `SsrfGuardTests` should verify the Contracts helper behavior directly, while still tolerating a temporary Core shim if needed during migration

    Do NOT migrate built-in module files in this plan. This plan only prepares the shared helper surfaces for later cohort plans.
  </action>
  <verify>
    <automated>bash -lc 'dotnet test /home/user/OpenAnima/tests/OpenAnima.Tests/ -q --filter "FullyQualifiedName~ContractsApiTests|FullyQualifiedName~SsrfGuardTests"'</automated>
  </verify>
  <done>`ModuleMetadataRecord` and `SsrfGuard` are defined in Contracts, their behavior is covered by tests, and temporary Core shims keep the repo compiling until later plans update module consumers.</done>
</task>

</tasks>

<success_criteria>
- [ ] Canonical Phase 36 wording reflects the real 12-module active inventory
- [ ] Helper files are no longer confused with module count in active docs
- [ ] LLMModule exception is documented explicitly
- [ ] ModuleMetadataRecord is available from Contracts
- [ ] SsrfGuard is available from Contracts
- [ ] Transitional Core shims preserve compile compatibility
</success_criteria>
