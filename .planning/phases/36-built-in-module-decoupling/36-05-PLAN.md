---
phase: 36-built-in-module-decoupling
plan: 05
type: execute
wave: 4
depends_on: ["36-02", "36-03", "36-04"]
files_modified:
  - tests/OpenAnima.Tests/Integration/BuiltInModuleDecouplingTests.cs
  - tests/OpenAnima.Tests/Integration/ModuleRuntimeInitializationTests.cs
autonomous: true
requirements: [DECPL-01, DECPL-02, DECPL-03]

must_haves:
  truths:
    - "The authoritative active built-in inventory is codified in tests as 12 live module files, not 14"
    - "Source-level decoupling is verified by automated checks, with only the documented LLMModule Core.LLM exception allowed"
    - "Every registered built-in module resolves from the real DI container/startup path without InvalidOperationException"
    - "The full OpenAnima test suite still passes after all migrations, with no accepted carry-forward CLI failures"
  artifacts:
    - path: "tests/OpenAnima.Tests/Integration/BuiltInModuleDecouplingTests.cs"
      provides: "Inventory and source-level decoupling audit coverage"
      contains: "12"
    - path: "tests/OpenAnima.Tests/Integration/ModuleRuntimeInitializationTests.cs"
      provides: "Real startup/DI resolution coverage for all built-in modules"
      contains: "GetRequiredService"
  key_links:
    - from: "tests/OpenAnima.Tests/Integration/BuiltInModuleDecouplingTests.cs"
      to: "src/OpenAnima.Core/Modules/*Module.cs"
      via: "source audit of authoritative module files"
      pattern: "OpenAnima\\.Core\\."
    - from: "tests/OpenAnima.Tests/Integration/ModuleRuntimeInitializationTests.cs"
      to: "src/OpenAnima.Core/DependencyInjection/WiringServiceExtensions.cs"
      via: "resolution of every registered built-in module type"
      pattern: "AddSingleton<.*Module>"
---

<objective>
Codify the final decoupling outcome and prove startup safety with full regression coverage.

Purpose: Phase 36 is not done when the source files look clean. The repo needs an automated proof of the authoritative 12-module inventory, the documented LLM exception policy, successful DI startup resolution for all built-ins, and a full regression run across both test projects.

Output: New decoupling audit tests, expanded startup/DI tests, and a full-suite verification pass.
</objective>

<execution_context>
@/home/user/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-built-in-module-decoupling/36-CONTEXT.md
@.planning/phases/36-built-in-module-decoupling/36-RESEARCH.md
@.planning/phases/36-built-in-module-decoupling/36-01-PLAN.md
@.planning/phases/36-built-in-module-decoupling/36-02-PLAN.md
@.planning/phases/36-built-in-module-decoupling/36-03-PLAN.md
@.planning/phases/36-built-in-module-decoupling/36-04-PLAN.md

@tests/OpenAnima.Tests/Integration/ModuleRuntimeInitializationTests.cs
@src/OpenAnima.Core/DependencyInjection/WiringServiceExtensions.cs
@src/OpenAnima.Core/Hosting/WiringInitializationService.cs

<verification_note>
- The final active-module source audit should hard-code the authoritative 12 module files so helper files are not counted again.
- The LLM exception is allowed only if the docs were normalized in Plan 01; all other built-in module files should have zero `using OpenAnima.Core.*`.
- `OpenAnima.Cli.Tests` must be zero-fail by this point; the current CLI baseline is repaired in Plan 04 and is not treated as accepted noise here.
</verification_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add authoritative inventory and source-level decoupling audit tests</name>
  <files>
    tests/OpenAnima.Tests/Integration/BuiltInModuleDecouplingTests.cs
  </files>
  <action>
    Create a new integration-style audit test file that codifies the Phase 36 contract in source form.

    The new tests must:
    - hard-code the 12 active built-in module source files as the authoritative inventory
    - assert helper files (`FormatDetector.cs`, `ModuleMetadataRecord.cs`) are not counted as active built-in modules
    - scan the 12 module files for `using OpenAnima.Core.` and fail for every module except the documented `LLMModule` exception
    - if `LLMModule` still imports `OpenAnima.Core.LLM`, assert that this is the ONLY remaining Core using in that file

    Keep this as a focused decoupling audit; do not turn it into a giant reflection test harness.
  </action>
  <verify>
    <automated>bash -lc 'dotnet test /home/user/OpenAnima/tests/OpenAnima.Tests/ -q --filter "FullyQualifiedName~BuiltInModuleDecouplingTests"'</automated>
  </verify>
  <done>The repo has an automated, hard-coded proof of the real 12-module inventory and the allowed source-level decoupling exception policy.</done>
</task>

<task type="auto">
  <name>Task 2: Extend startup tests to resolve all built-ins from DI and run the full regression suite</name>
  <files>
    tests/OpenAnima.Tests/Integration/ModuleRuntimeInitializationTests.cs
  </files>
  <action>
    Expand `ModuleRuntimeInitializationTests` so the test setup can resolve every built-in module registered by `AddWiringServices`.

    Required coverage:
    - assert the resolved built-in set matches the authoritative 12-module inventory
    - resolve each registered built-in module type from the real DI container without `InvalidOperationException`
    - keep the existing port-registration and initialization tests intact

    Add whatever real service registrations the startup test fixture still needs for the migrated modules to resolve cleanly (for example `HttpClientFactory`, config services, or other runtime dependencies introduced by the migration). Use real project registrations where feasible, not ad hoc fake implementations, unless a dependency is unrelated to the DI surface being validated.

    After the targeted startup test passes, run the full regression suite for both test projects as the plan's automated proof.

    Treat any remaining CLI failures as blockers. Do not waive them as "pre-existing" at this stage; Plan 04 is responsible for clearing the live CLI baseline before this final verification wave runs.
  </action>
  <verify>
    <automated>bash -lc 'dotnet test /home/user/OpenAnima/tests/OpenAnima.Tests/ -q && dotnet test /home/user/OpenAnima/tests/OpenAnima.Cli.Tests/ -q'</automated>
  </verify>
  <done>Startup/DI tests prove all built-in modules resolve successfully, and the full OpenAnima test suite passes after the entire migration.</done>
</task>

</tasks>

<success_criteria>
- [ ] The real 12-module inventory is enforced by automated tests
- [ ] Source-level decoupling is checked automatically, with only the documented LLM exception allowed
- [ ] All registered built-in modules resolve from real DI startup tests
- [ ] Full OpenAnima.Tests and OpenAnima.Cli.Tests suites pass
</success_criteria>
