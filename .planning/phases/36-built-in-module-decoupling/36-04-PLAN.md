---
phase: 36-built-in-module-decoupling
plan: 04
type: execute
wave: 3
depends_on: ["36-01", "36-02", "36-03"]
files_modified:
  - src/OpenAnima.Core/Modules/LLMModule.cs
  - src/OpenAnima.Cli/Templates/module-cs.tmpl
  - src/OpenAnima.Cli/Templates/module-csproj.tmpl
  - tests/OpenAnima.Tests/Modules/ModuleTests.cs
  - tests/OpenAnima.Tests/Integration/ModulePipelineIntegrationTests.cs
  - tests/OpenAnima.Tests/Integration/PromptInjectionIntegrationTests.cs
  - tests/OpenAnima.Tests/Integration/ChatPanelModulePipelineTests.cs
  - tests/OpenAnima.Cli.Tests/CliFoundationTests.cs
autonomous: true
requirements: [DECPL-01, DECPL-03, DECPL-04]

must_haves:
  truths:
    - "LLMModule uses Contracts metadata/config/context/routing imports and retains only the documented OpenAnima.Core.LLM dependency"
    - "Prompt injection, routing-marker detection, retry behavior, and per-Anima config override behavior remain intact"
    - "`oani new` generates Contracts-only code using the shared metadata pattern and does not introduce any OpenAnima.Core reference"
    - "The current OpenAnima.Cli.Tests baseline is repaired inside this plan so the final DECPL-03 full-suite gate is honest"
    - "Template output stays minimal and readable while demonstrating the modern Contracts-first module shape"
  artifacts:
    - path: "src/OpenAnima.Core/Modules/LLMModule.cs"
      provides: "Documented LLM exception module with Contracts-first imports elsewhere"
      contains: "using OpenAnima.Core.LLM"
    - path: "src/OpenAnima.Cli/Templates/module-cs.tmpl"
      provides: "Contracts-only module template"
      contains: "ModuleMetadataRecord"
    - path: "tests/OpenAnima.Cli.Tests/CliFoundationTests.cs"
      provides: "Template/new-command assertions plus deterministic CLI console-capture coverage"
      contains: "RunCliWithArgs"
  key_links:
    - from: "src/OpenAnima.Core/Modules/LLMModule.cs"
      to: "OpenAnima.Contracts.Routing"
      via: "route discovery and port metadata access"
      pattern: "GetPortsForAnima"
    - from: "src/OpenAnima.Cli/Templates/module-cs.tmpl"
      to: "src/OpenAnima.Contracts/ModuleMetadataRecord.cs"
      via: "generated metadata construction"
      pattern: "ModuleMetadataRecord"
---

<objective>
Finish the remaining module/template decoupling work by handling the explicit LLM exception cleanly and modernizing the CLI scaffolding.

Purpose: `LLMModule` is the only allowed Core-dependent built-in after the rest of Phase 36, and the CLI template must reflect the same Contracts-first module shape that the built-ins now model. This same wave must also repair the currently failing CLI test baseline so the phase can finish on a real full-suite pass rather than a waived exception.

Output: `LLMModule` is Contracts-first everywhere except `OpenAnima.Core.LLM`, `oani new` produces a minimal Contracts-only module template aligned with the migrated built-ins, and the CLI test project is back to a clean baseline.
</objective>

<execution_context>
@/home/user/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-built-in-module-decoupling/36-CONTEXT.md
@.planning/phases/36-built-in-module-decoupling/36-RESEARCH.md
@.planning/phases/36-built-in-module-decoupling/36-01-PLAN.md

@src/OpenAnima.Core/Modules/LLMModule.cs
@src/OpenAnima.Cli/Templates/module-cs.tmpl
@src/OpenAnima.Cli/Templates/module-csproj.tmpl
@tests/OpenAnima.Tests/Modules/ModuleTests.cs
@tests/OpenAnima.Tests/Integration/ModulePipelineIntegrationTests.cs
@tests/OpenAnima.Tests/Integration/PromptInjectionIntegrationTests.cs
@tests/OpenAnima.Tests/Integration/ChatPanelModulePipelineTests.cs
@tests/OpenAnima.Cli.Tests/CliFoundationTests.cs

<exception_note>
- The user-approved exception is narrow: keep `ILLMService` and its DTOs in Core for now.
- Do NOT expand this plan into a general LLM Contracts migration.
</exception_note>

<cli_note>
- The live CLI baseline is currently `67 passed / 7 failed`.
- Failures cluster around `CliFoundationTests.cs` console-capture helpers and must be repaired in this plan rather than deferred to the final verification wave.
</cli_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make LLMModule Contracts-first everywhere except the documented Core.LLM surface</name>
  <files>
    src/OpenAnima.Core/Modules/LLMModule.cs,
    tests/OpenAnima.Tests/Modules/ModuleTests.cs,
    tests/OpenAnima.Tests/Integration/ModulePipelineIntegrationTests.cs,
    tests/OpenAnima.Tests/Integration/PromptInjectionIntegrationTests.cs,
    tests/OpenAnima.Tests/Integration/ChatPanelModulePipelineTests.cs
  </files>
  <action>
    Update `LLMModule` so it uses Contracts for:
    - `ModuleMetadataRecord`
    - `IModuleConfig`
    - `IModuleContext`
    - `OpenAnima.Contracts.Routing`

    Keep `OpenAnima.Core.LLM` as the only remaining Core import in the file. Do NOT move `ILLMService`, `ChatMessageInput`, or `LLMResult` in this plan.

    Preserve all behavior:
    - per-Anima config override lookup
    - route-service prompt injection
    - `FormatDetector` parsing
    - retry/self-correction loop
    - existing response/error publish paths

    Update the focused LLM/chat test files only where constructor signatures or namespace imports require it. Prefer small fixture tweaks over rewriting assertions.
  </action>
  <verify>
    <automated>bash -lc 'dotnet test /home/user/OpenAnima/tests/OpenAnima.Tests/ -q --filter "FullyQualifiedName~ModuleTests|FullyQualifiedName~ModulePipelineIntegrationTests|FullyQualifiedName~PromptInjectionIntegrationTests|FullyQualifiedName~ChatPanelModulePipelineTests"'</automated>
  </verify>
  <done>LLMModule is Contracts-first except for its explicit Core.LLM dependency, and all LLM/chat regression tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Modernize `oani new` and restore the CLI test baseline</name>
  <files>
    src/OpenAnima.Cli/Templates/module-cs.tmpl,
    src/OpenAnima.Cli/Templates/module-csproj.tmpl,
    tests/OpenAnima.Cli.Tests/CliFoundationTests.cs
  </files>
  <action>
    Update the C# module template so generated code:
    - stays Contracts-only
    - uses `ModuleMetadataRecord` instead of an inline metadata class
    - remains minimal and readable
    - demonstrates the modern module shape without dragging in Core references

    Recommended template direction:
    - keep the generated class small
    - if you include capability examples, use optional Contracts-only constructor parameters such as `IModuleConfig?`, `IModuleContext?`, and `ICrossAnimaRouter?`
    - do NOT generate bulky tutorial-style scaffolding or verbose comments

    Keep the generated project file Contracts-only. If the existing `.csproj` template already satisfies that requirement, only adjust it where the new generated C# shape needs matching imports or comments.

    Update template and CLI tests to assert:
    - generated C# references Contracts only
    - generated metadata uses `ModuleMetadataRecord`
    - no `OpenAnima.Core` reference appears in generated output

    Repair the current CLI baseline inside `CliFoundationTests.cs` rather than punting it to the final wave:
    - make the stdout/stderr capture helpers deterministic and safe to run across the whole CLI suite
    - prevent `Console.SetOut` / `Console.SetError` races or disposed-writer reuse from causing false failures
    - preserve existing `pack`, `validate`, `help`, and `new` command assertions unless the template/output contract intentionally changes as part of this plan
    - do not weaken or delete affected CLI assertions just to force a green suite
  </action>
  <verify>
    <automated>bash -lc 'dotnet test /home/user/OpenAnima/tests/OpenAnima.Cli.Tests/ -q'</automated>
  </verify>
  <done>`oani new` produces a minimal Contracts-first module template aligned with the migrated built-ins, and the full CLI test project is green again.</done>
</task>

</tasks>

<success_criteria>
- [ ] LLMModule uses Contracts imports everywhere except the documented Core.LLM dependency
- [ ] LLM/chat regression tests pass
- [ ] CLI template uses ModuleMetadataRecord from Contracts
- [ ] Generated template code has no OpenAnima.Core references
- [ ] OpenAnima.Cli.Tests returns to a clean baseline in this wave
- [ ] Template output remains concise and readable
</success_criteria>
