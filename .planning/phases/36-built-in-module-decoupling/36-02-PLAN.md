---
phase: 36-built-in-module-decoupling
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - src/OpenAnima.Core/Modules/ChatInputModule.cs
  - src/OpenAnima.Core/Modules/ChatOutputModule.cs
  - src/OpenAnima.Core/Modules/HeartbeatModule.cs
  - src/OpenAnima.Core/Modules/FixedTextModule.cs
  - src/OpenAnima.Core/Modules/TextJoinModule.cs
  - src/OpenAnima.Core/Modules/TextSplitModule.cs
  - src/OpenAnima.Core/Modules/ConditionalBranchModule.cs
autonomous: true
requirements: [DECPL-01]

must_haves:
  truths:
    - "ChatInputModule, ChatOutputModule, HeartbeatModule, FixedTextModule, TextJoinModule, TextSplitModule, and ConditionalBranchModule no longer import OpenAnima.Core.* namespaces"
    - "ChatInputModule no longer carries the dead ActivityChannelHost dependency; its direct EventBus publish path remains intact"
    - "All seven modules use ModuleMetadataRecord from Contracts rather than Core.Modules"
    - "Config-driven modules use IModuleConfig and IModuleContext instead of Core shim interfaces"
    - "Port names, event names, state transitions, and existing behavior remain unchanged"
  artifacts:
    - path: "src/OpenAnima.Core/Modules/ChatInputModule.cs"
      provides: "Contracts-only source module with no dead channel-host hook"
      contains: "PublishAsync"
    - path: "src/OpenAnima.Core/Modules/FixedTextModule.cs"
      provides: "Contracts-only config/context usage for template publishing"
      contains: "IModuleConfig"
    - path: "src/OpenAnima.Core/Modules/ConditionalBranchModule.cs"
      provides: "Contracts-only config/context usage with existing concurrency guard"
      contains: "IModuleContext"
  key_links:
    - from: "src/OpenAnima.Core/Modules/ChatInputModule.cs"
      to: "src/OpenAnima.Contracts/IEventBus.cs"
      via: "direct user-message publish"
      pattern: "ChatInputModule\\.port\\.userMessage"
    - from: "src/OpenAnima.Core/Modules/FixedTextModule.cs"
      to: "src/OpenAnima.Contracts/ModuleMetadataRecord.cs"
      via: "shared metadata construction"
      pattern: "new ModuleMetadataRecord"
---

<objective>
Migrate the low-risk built-in module cohort to Contracts-only imports without changing behavior.

Purpose: These source/sink/text modules are the cleanest place to remove Core namespace dependencies and adopt the shared Contracts metadata/config/context surfaces. The only special case is `ChatInputModule`, whose `ActivityChannelHost` hook is dead code and should be removed rather than abstracted.

Output: Seven built-in module files use Contracts-only imports and keep their current runtime behavior.
</objective>

<execution_context>
@/home/user/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-built-in-module-decoupling/36-CONTEXT.md
@.planning/phases/36-built-in-module-decoupling/36-RESEARCH.md
@.planning/phases/36-built-in-module-decoupling/36-01-PLAN.md

@src/OpenAnima.Core/Modules/ChatInputModule.cs
@src/OpenAnima.Core/Modules/ChatOutputModule.cs
@src/OpenAnima.Core/Modules/HeartbeatModule.cs
@src/OpenAnima.Core/Modules/FixedTextModule.cs
@src/OpenAnima.Core/Modules/TextJoinModule.cs
@src/OpenAnima.Core/Modules/TextSplitModule.cs
@src/OpenAnima.Core/Modules/ConditionalBranchModule.cs

<migration_note>
- `ChatInputModule.SetChannelHost(...)` currently has no call site anywhere in the repo; remove it instead of designing a new contract.
- `FixedTextModule`, `TextJoinModule`, `TextSplitModule`, and `ConditionalBranchModule` only need the Contracts config/context interfaces and the moved metadata record.
</migration_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Core-only plumbing from source/sink/heartbeat modules</name>
  <files>
    src/OpenAnima.Core/Modules/ChatInputModule.cs,
    src/OpenAnima.Core/Modules/ChatOutputModule.cs,
    src/OpenAnima.Core/Modules/HeartbeatModule.cs
  </files>
  <action>
    Update all three files to use the Contracts-owned `ModuleMetadataRecord` and remove any remaining `using OpenAnima.Core.*` directives.

    For `ChatInputModule` specifically:
    - Delete the `ActivityChannelHost` field, the `SetChannelHost(...)` method, and the conditional enqueue branch in `SendMessageAsync(...)`
    - Keep the existing direct `EventBus.PublishAsync(...)` path as the only behavior
    - Rewrite comments so they describe the actual remaining behavior rather than the dead channel-host path

    For `ChatOutputModule` and `HeartbeatModule`:
    - Keep port attributes, event names, state transitions, and public API unchanged
    - Only change imports/metadata construction as needed for Contracts ownership
  </action>
  <verify>
    <automated>bash -lc 'dotnet test /home/user/OpenAnima/tests/OpenAnima.Tests/ -q --filter "FullyQualifiedName~ModuleTests|FullyQualifiedName~ModulePipelineIntegrationTests"'</automated>
  </verify>
  <done>The three modules compile without any OpenAnima.Core using directives, and chat/heartbeat regression tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Move text and branch utility modules to Contracts config/context surfaces</name>
  <files>
    src/OpenAnima.Core/Modules/FixedTextModule.cs,
    src/OpenAnima.Core/Modules/TextJoinModule.cs,
    src/OpenAnima.Core/Modules/TextSplitModule.cs,
    src/OpenAnima.Core/Modules/ConditionalBranchModule.cs
  </files>
  <action>
    Update all four files to:
    - replace `IAnimaModuleConfigService` with `IModuleConfig`
    - replace `IAnimaContext` with `IModuleContext`
    - use `ModuleMetadataRecord` from Contracts
    - remove `using OpenAnima.Core.Anima` and `using OpenAnima.Core.Services`

    Keep the existing business logic exactly as-is:
    - `FixedTextModule` still interpolates `{{key}}` placeholders from config
    - `TextJoinModule` still orders buffered inputs by key and clears after publish
    - `TextSplitModule` still outputs JSON arrays with the current delimiter behavior
    - `ConditionalBranchModule` still uses the current expression evaluator and semaphore guard

    Do NOT redesign these modules into new abstractions or add new config schema work in this plan.
  </action>
  <verify>
    <automated>bash -lc 'dotnet test /home/user/OpenAnima/tests/OpenAnima.Tests/ -q --filter "FullyQualifiedName~ConcurrencyGuardTests|FullyQualifiedName~ActivityChannelSoakTests|FullyQualifiedName~ModulePipelineIntegrationTests"'</automated>
  </verify>
  <done>All seven modules in this cohort are Contracts-only at the source-file level, and their existing behavior/regression tests remain green.</done>
</task>

</tasks>

<success_criteria>
- [ ] No OpenAnima.Core using directives remain in the 7 cohort module files
- [ ] ChatInputModule dead channel-host path is removed
- [ ] Metadata comes from Contracts in every cohort module
- [ ] Config/context usage in text modules uses Contracts interfaces
- [ ] Existing behavior and tests remain intact
</success_criteria>
