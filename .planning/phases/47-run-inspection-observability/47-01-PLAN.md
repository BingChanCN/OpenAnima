---
phase: 47-run-inspection-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/OpenAnima.Tests/Unit/RunDetailTimelineTests.cs
  - tests/OpenAnima.Tests/Unit/PropagationColorTests.cs
  - tests/OpenAnima.Tests/Unit/WiringEngineScopeTests.cs
  - src/OpenAnima.Core/Runs/PropagationColorAssigner.cs
  - src/OpenAnima.Core/Components/Shared/RunCard.razor
  - src/OpenAnima.Core/Wiring/WiringEngine.cs
  - src/OpenAnima.Core/Runs/RunService.cs
  - src/OpenAnima.Core/Resources/SharedResources.en-US.resx
  - src/OpenAnima.Core/Resources/SharedResources.zh-CN.resx
autonomous: true
requirements: [OBS-01, OBS-03, OBS-04]
must_haves:
  truths:
    - "PropagationColorAssigner returns deterministic color hex for a given PropagationId"
    - "PropagationColorAssigner cycles through 8 colors and handles empty/null input"
    - "WiringEngine wraps step execution in ILogger.BeginScope with RunId and StepId"
    - "RunService wraps lifecycle methods in ILogger.BeginScope with RunId"
    - "RunCard click navigates to /runs/{RunId}"
    - "All RunDetail localization keys exist in both en-US and zh-CN resource files"
  artifacts:
    - path: "src/OpenAnima.Core/Runs/PropagationColorAssigner.cs"
      provides: "Deterministic PropagationId-to-color mapping"
      exports: ["PropagationColorAssigner"]
    - path: "tests/OpenAnima.Tests/Unit/PropagationColorTests.cs"
      provides: "Unit tests for color assignment"
    - path: "tests/OpenAnima.Tests/Unit/RunDetailTimelineTests.cs"
      provides: "Unit tests for timeline merge logic"
    - path: "tests/OpenAnima.Tests/Unit/WiringEngineScopeTests.cs"
      provides: "Unit tests for ILogger.BeginScope injection"
  key_links:
    - from: "src/OpenAnima.Core/Wiring/WiringEngine.cs"
      to: "ILogger.BeginScope"
      via: "using statement wrapping step recording"
      pattern: "_logger\\.BeginScope.*RunId.*StepId"
    - from: "src/OpenAnima.Core/Components/Shared/RunCard.razor"
      to: "/runs/{RunId}"
      via: "NavigationManager.NavigateTo"
      pattern: "NavigateTo.*runs/"
---

<objective>
Lay the foundation for Phase 47: test scaffolds, shared utility, navigation wiring, localization keys, and backend log correlation.

Purpose: Provide the test infrastructure (Wave 0), the PropagationColorAssigner utility consumed by the UI plans, RunCard click-to-detail navigation, all localization keys needed by the RunDetail page, and ILogger.BeginScope injection for developer log correlation (OBS-04).

Output: 3 test files, 1 utility class, updated RunCard, updated WiringEngine/RunService with BeginScope, updated resource files.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-run-inspection-observability/47-CONTEXT.md
@.planning/phases/47-run-inspection-observability/47-RESEARCH.md
@.planning/phases/47-run-inspection-observability/47-UI-SPEC.md
@.planning/phases/47-run-inspection-observability/47-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/OpenAnima.Core/Runs/StepRecord.cs:
StepRecord record with: StepId, RunId, PropagationId, ModuleName, Status, InputSummary, OutputSummary, ArtifactRefId, ErrorInfo, DurationMs, OccurredAt (all string props, DurationMs is int?)

From src/OpenAnima.Core/Runs/RunStateEvent.cs:
RunStateEvent record with: Id (long), RunId, State, Reason (nullable), OccurredAt (all strings)

From src/OpenAnima.Core/Hubs/IRuntimeClient.cs:
Task ReceiveRunStateChanged(string animaId, string runId, string state, string? reason)
Task ReceiveStepCompleted(string animaId, string runId, string stepId, string moduleName, string status, int? durationMs)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test scaffolds + PropagationColorAssigner utility</name>
  <files>
    tests/OpenAnima.Tests/Unit/PropagationColorTests.cs,
    tests/OpenAnima.Tests/Unit/RunDetailTimelineTests.cs,
    tests/OpenAnima.Tests/Unit/WiringEngineScopeTests.cs,
    src/OpenAnima.Core/Runs/PropagationColorAssigner.cs
  </files>
  <read_first>
    src/OpenAnima.Core/Runs/StepRecord.cs,
    src/OpenAnima.Core/Runs/RunStateEvent.cs,
    tests/OpenAnima.Tests/Unit/RunServiceTests.cs,
    tests/OpenAnima.Tests/OpenAnima.Tests.csproj
  </read_first>
  <behavior>
    PropagationColorTests:
    - GetColor("abc123") returns a string starting with "#" and length 7
    - GetColor("abc123") called twice returns the same value (deterministic)
    - GetColor("") returns "transparent"
    - GetColor(null) returns "transparent"
    - GetColor with 8+ distinct propagationIds cycles through exactly these hex values: #6c8cff, #4ade80, #fbbf24, #f87171, #a78bfa, #34d399, #fb923c, #60a5fa
    - Color index is computed as Math.Abs(propagationId.GetHashCode()) % 8

    RunDetailTimelineTests:
    - MergeTimeline with 2 steps and 2 state events returns 4 entries sorted by OccurredAt ascending
    - MergeTimeline with empty steps and 1 state event returns 1 entry
    - MergeTimeline with overlapping timestamps preserves stable sort (state events before steps at same timestamp)
    - Each merged entry has: OccurredAt (string), IsStateEvent (bool), StepRecord? and RunStateEvent? (one null, one populated)

    WiringEngineScopeTests:
    - Stub test that verifies ILogger.BeginScope is called with dictionary containing "RunId" key during step recording path (will be fleshed out after Task 2 modifies WiringEngine)
  </behavior>
  <action>
    1. Create `src/OpenAnima.Core/Runs/PropagationColorAssigner.cs`:
       - Static class `PropagationColorAssigner` in namespace `OpenAnima.Core.Runs`
       - Private static readonly string array `Colors` with 8 entries: "#6c8cff", "#4ade80", "#fbbf24", "#f87171", "#a78bfa", "#34d399", "#fb923c", "#60a5fa"
       - Public static method `string GetColor(string? propagationId)`:
         - If `string.IsNullOrEmpty(propagationId)` return `"transparent"`
         - Else return `Colors[Math.Abs(propagationId.GetHashCode()) % Colors.Length]`

    2. Create `tests/OpenAnima.Tests/Unit/PropagationColorTests.cs`:
       - Namespace `OpenAnima.Tests.Unit`
       - Test class with `[Fact]` tests for each behavior above
       - Use `using OpenAnima.Core.Runs;`

    3. Create `tests/OpenAnima.Tests/Unit/RunDetailTimelineTests.cs`:
       - Define a local helper record `TimelineEntry` with properties: `string OccurredAt`, `bool IsStateEvent`, `StepRecord? Step`, `RunStateEvent? StateEvent`
       - Define a local static method `MergeTimeline(IReadOnlyList<StepRecord> steps, IReadOnlyList<RunStateEvent> stateEvents)` that:
         - Maps each StepRecord to TimelineEntry with IsStateEvent=false, OccurredAt=step.OccurredAt
         - Maps each RunStateEvent to TimelineEntry with IsStateEvent=true, OccurredAt=stateEvent.OccurredAt
         - Concatenates and sorts by OccurredAt ascending, with IsStateEvent=true entries first on ties (OrderBy OccurredAt ThenBy IsStateEvent descending)
       - Tests per behavior above using `[Fact]`
       - NOTE: This merge logic will later be extracted into the RunDetail page. The test validates the algorithm standalone.

    4. Create `tests/OpenAnima.Tests/Unit/WiringEngineScopeTests.cs`:
       - Namespace `OpenAnima.Tests.Unit`
       - Single `[Fact]` test `BeginScope_CalledWithRunIdAndStepId_DuringStepRecording` that is initially marked `[Fact(Skip = "Implemented in Task 2")]` as a placeholder
       - Import `OpenAnima.Core.Wiring` and `OpenAnima.Core.Runs`
  </action>
  <verify>
    <automated>cd /home/user/OpenAnima && dotnet test tests/OpenAnima.Tests/ --filter "FullyQualifiedName~PropagationColorTests OR FullyQualifiedName~RunDetailTimelineTests" --no-restore -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/OpenAnima.Tests/Unit/PropagationColorTests.cs exists and contains `class PropagationColorTests`
    - tests/OpenAnima.Tests/Unit/RunDetailTimelineTests.cs exists and contains `class RunDetailTimelineTests`
    - tests/OpenAnima.Tests/Unit/WiringEngineScopeTests.cs exists and contains `class WiringEngineScopeTests`
    - src/OpenAnima.Core/Runs/PropagationColorAssigner.cs contains `public static string GetColor(string? propagationId)`
    - src/OpenAnima.Core/Runs/PropagationColorAssigner.cs contains `"#6c8cff", "#4ade80", "#fbbf24", "#f87171", "#a78bfa", "#34d399", "#fb923c", "#60a5fa"`
    - PropagationColorTests pass (dotnet test exits 0)
    - RunDetailTimelineTests pass (dotnet test exits 0)
  </acceptance_criteria>
  <done>PropagationColorAssigner utility created with deterministic color cycling. Three test files created. All non-skipped tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: RunCard navigation + ILogger.BeginScope + localization keys</name>
  <files>
    src/OpenAnima.Core/Components/Shared/RunCard.razor,
    src/OpenAnima.Core/Wiring/WiringEngine.cs,
    src/OpenAnima.Core/Runs/RunService.cs,
    src/OpenAnima.Core/Resources/SharedResources.en-US.resx,
    src/OpenAnima.Core/Resources/SharedResources.zh-CN.resx,
    tests/OpenAnima.Tests/Unit/WiringEngineScopeTests.cs
  </files>
  <read_first>
    src/OpenAnima.Core/Components/Shared/RunCard.razor,
    src/OpenAnima.Core/Wiring/WiringEngine.cs,
    src/OpenAnima.Core/Runs/RunService.cs,
    src/OpenAnima.Core/Resources/SharedResources.en-US.resx,
    src/OpenAnima.Core/Resources/SharedResources.zh-CN.resx,
    src/OpenAnima.Core/Runs/IStepRecorder.cs,
    src/OpenAnima.Core/Runs/StepRecorder.cs,
    src/OpenAnima.Core/Hubs/IRuntimeClient.cs,
    tests/OpenAnima.Tests/Unit/WiringEngineScopeTests.cs
  </read_first>
  <action>
    **RunCard.razor changes:**
    1. Add `@inject NavigationManager Navigation` at the top (after existing @using)
    2. Add `@onclick="HandleClick"` and `style="cursor: pointer;"` to the outer `<div class="run-card">` element
    3. Add `role="link"` and `tabindex="0"` and `@onkeydown="HandleKeyDown"` to the outer div for accessibility
    4. Add method in @code block:
       ```csharp
       private void HandleClick() => Navigation.NavigateTo($"/runs/{Run.RunId}");
       private void HandleKeyDown(KeyboardEventArgs e)
       {
           if (e.Key == "Enter" || e.Key == " ")
               Navigation.NavigateTo($"/runs/{Run.RunId}");
       }
       ```
    5. Ensure button clicks still work (they already have @onclick:stopPropagation is NOT set, so add `@onclick:stopPropagation="true"` to each existing button to prevent card navigation when clicking Pause/Resume/Cancel)

    **WiringEngine.cs changes (ILogger.BeginScope for OBS-04):**
    In the `CreateRoutingSubscription` method, inside both the `PortType.Text` and `PortType.Trigger` lambda handlers, wrap the step recording + forwarding block in an `ILogger.BeginScope`. Specifically, after `await semaphore.WaitAsync(ct);` and inside the try block, before the stepId assignment:

    ```csharp
    // Inside the try block, before existing step recording code:
    var scopeRunId = _stepRecorder != null ? "active" : "none";
    var scopeStepId = "pending";
    // After stepId is assigned, update scopeStepId — but BeginScope must wrap the whole block.
    // Use a dictionary scope:
    using var logScope = _logger.BeginScope(new Dictionary<string, object?>
    {
        ["RunId"] = _animaId,  // animaId serves as run correlation at this level
        ["SourceModule"] = sourceModuleRuntimeName,
        ["TargetModule"] = targetModuleRuntimeName
    });
    ```

    Actually, the correct approach: wrap the entire try block contents after semaphore acquisition. The stepId is only known after RecordStepStartAsync, so use two scopes — one outer with module context, one inner with stepId after it's known:

    ```csharp
    // After semaphore.WaitAsync, at start of try block:
    using var routeScope = _logger.BeginScope(new Dictionary<string, object?>
    {
        ["SourceModule"] = sourceModuleRuntimeName,
        ["TargetModule"] = targetModuleRuntimeName
    });

    // ... existing inputSummary and stepId assignment ...

    // After stepId is assigned (and is not null), add inner scope:
    using var stepScope = stepId != null
        ? _logger.BeginScope(new Dictionary<string, object?> { ["StepId"] = stepId })
        : null;
    ```

    Apply this pattern to BOTH the `PortType.Text` and `PortType.Trigger` branches in `CreateRoutingSubscription`.

    **RunService.cs changes (ILogger.BeginScope for OBS-04):**
    In `StartRunAsync`, after `var runId = ...` line, add:
    ```csharp
    using var scope = _logger.BeginScope(new Dictionary<string, object?> { ["RunId"] = runId, ["AnimaId"] = animaId });
    ```

    In `PauseRunAsync`, at method start add:
    ```csharp
    using var scope = _logger.BeginScope(new Dictionary<string, object?> { ["RunId"] = runId });
    ```

    In `ResumeRunAsync`, at method start add:
    ```csharp
    using var scope = _logger.BeginScope(new Dictionary<string, object?> { ["RunId"] = runId });
    ```

    In `CancelRunAsync`, at method start add:
    ```csharp
    using var scope = _logger.BeginScope(new Dictionary<string, object?> { ["RunId"] = runId });
    ```

    **WiringEngineScopeTests.cs update:**
    Remove the `Skip` attribute from the placeholder test. Implement a real test:
    - Create a mock ILogger<WiringEngine> that captures BeginScope calls
    - Create a WiringEngine with the mock logger, a real EventBus, a real PortRegistry, and a mock IStepRecorder
    - Load a simple 2-node configuration with one Text connection
    - Publish an event on the source port
    - Assert that _logger.BeginScope was called with a dictionary containing "SourceModule" and "TargetModule" keys

    **Localization keys (SharedResources.en-US.resx and SharedResources.zh-CN.resx):**
    Add all 22 keys from the UI-SPEC to BOTH resource files. For zh-CN, use Chinese translations:

    en-US values:
    - RunDetail.PageTitle = Run Detail
    - RunDetail.BackLink = ← Runs
    - RunDetail.TimelineHeading = Timeline
    - RunDetail.Filter.Module = Module
    - RunDetail.Filter.Status = Status
    - RunDetail.Filter.Chain = Chain
    - RunDetail.Filter.AllModules = All modules
    - RunDetail.Filter.AllStatuses = All statuses
    - RunDetail.Filter.AllChains = All chains
    - RunDetail.Filter.ClearFilters = Clear filters
    - RunDetail.EmptyTimeline.Heading = No steps yet
    - RunDetail.EmptyTimeline.Body = Steps will appear here as the run executes.
    - RunDetail.EmptyFilter.Heading = No matching steps
    - RunDetail.EmptyFilter.Body = Try adjusting the filters above.
    - RunDetail.NotFound.Heading = Run not found
    - RunDetail.NotFound.Body = This run ID does not exist or has been removed.
    - RunDetail.Step.Input = Input
    - RunDetail.Step.Output = Output
    - RunDetail.Step.ArtifactLink = View full content ↗
    - RunDetail.Step.ArtifactLinkTitle = Artifact viewer coming in a future release
    - RunDetail.Step.PropagationChain = Propagation chain: {0} — {1} steps
    - RunDetail.LongTimelineBanner = Showing all {0} steps. Use filters to narrow the view.

    zh-CN values:
    - RunDetail.PageTitle = 运行详情
    - RunDetail.BackLink = ← 运行列表
    - RunDetail.TimelineHeading = 时间线
    - RunDetail.Filter.Module = 模块
    - RunDetail.Filter.Status = 状态
    - RunDetail.Filter.Chain = 传播链
    - RunDetail.Filter.AllModules = 所有模块
    - RunDetail.Filter.AllStatuses = 所有状态
    - RunDetail.Filter.AllChains = 所有传播链
    - RunDetail.Filter.ClearFilters = 清除筛选
    - RunDetail.EmptyTimeline.Heading = 暂无步骤
    - RunDetail.EmptyTimeline.Body = 运行执行时，步骤将显示在此处。
    - RunDetail.EmptyFilter.Heading = 无匹配步骤
    - RunDetail.EmptyFilter.Body = 请尝试调整上方的筛选条件。
    - RunDetail.NotFound.Heading = 未找到运行
    - RunDetail.NotFound.Body = 此运行 ID 不存在或已被删除。
    - RunDetail.Step.Input = 输入
    - RunDetail.Step.Output = 输出
    - RunDetail.Step.ArtifactLink = 查看完整内容 ↗
    - RunDetail.Step.ArtifactLinkTitle = 制品查看器将在未来版本中推出
    - RunDetail.Step.PropagationChain = 传播链: {0} — {1} 步
    - RunDetail.LongTimelineBanner = 显示全部 {0} 个步骤。使用筛选缩小范围。
  </action>
  <verify>
    <automated>cd /home/user/OpenAnima && dotnet build src/OpenAnima.Core/ --no-restore && dotnet test tests/OpenAnima.Tests/ --filter "FullyQualifiedName~WiringEngineScopeTests" --no-restore -x</automated>
  </verify>
  <acceptance_criteria>
    - RunCard.razor contains `NavigateTo($"/runs/{Run.RunId}")`
    - RunCard.razor contains `@onclick:stopPropagation="true"` on action buttons
    - RunCard.razor contains `role="link"` on outer div
    - WiringEngine.cs contains `_logger.BeginScope(new Dictionary<string, object?>`
    - WiringEngine.cs contains `["SourceModule"]` and `["TargetModule"]` and `["StepId"]`
    - RunService.cs StartRunAsync contains `_logger.BeginScope` with `["RunId"]`
    - RunService.cs PauseRunAsync contains `_logger.BeginScope` with `["RunId"]`
    - SharedResources.en-US.resx contains `RunDetail.PageTitle`
    - SharedResources.en-US.resx contains `RunDetail.Step.PropagationChain`
    - SharedResources.zh-CN.resx contains `RunDetail.PageTitle`
    - SharedResources.zh-CN.resx contains `运行详情`
    - WiringEngineScopeTests.cs does NOT contain `Skip =`
    - dotnet build exits 0
    - WiringEngineScopeTests pass
  </acceptance_criteria>
  <done>RunCard navigates to /runs/{RunId} on click. WiringEngine and RunService inject RunId/StepId/module context via ILogger.BeginScope. All 22 localization keys added to both resource files. WiringEngineScopeTests pass.</done>
</task>

</tasks>

<verification>
dotnet build src/OpenAnima.Core/ --no-restore
dotnet test tests/OpenAnima.Tests/ --filter "FullyQualifiedName~PropagationColor OR FullyQualifiedName~RunDetailTimeline OR FullyQualifiedName~WiringEngineScope" --no-restore -x
</verification>

<success_criteria>
- PropagationColorAssigner.GetColor returns deterministic hex colors cycling through 8-color palette
- RunCard click navigates to /runs/{RunId}
- WiringEngine routing path wraps step execution in ILogger.BeginScope with SourceModule, TargetModule, StepId
- RunService lifecycle methods wrap in ILogger.BeginScope with RunId
- All 22 RunDetail.* localization keys present in both en-US and zh-CN resource files
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/47-run-inspection-observability/47-01-SUMMARY.md`
</output>
