---
phase: 24-service-migration-i18n
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - src/OpenAnima.Core/Components/Pages/Dashboard.razor
  - src/OpenAnima.Core/Components/Pages/Modules.razor
  - src/OpenAnima.Core/Components/Pages/Heartbeat.razor
  - src/OpenAnima.Core/Components/Pages/Monitor.razor
  - src/OpenAnima.Core/Components/Pages/Editor.razor
  - src/OpenAnima.Core/Components/Shared/AnimaListPanel.razor
  - src/OpenAnima.Core/Components/Shared/AnimaCreateDialog.razor
  - src/OpenAnima.Core/Components/Shared/AnimaContextMenu.razor
  - src/OpenAnima.Core/Components/Shared/ConfirmDialog.razor
  - src/OpenAnima.Core/Components/Shared/ChatPanel.razor
  - src/OpenAnima.Core/Components/Shared/ChatInput.razor
  - src/OpenAnima.Core/Resources/SharedResources.zh-CN.resx
  - src/OpenAnima.Core/Resources/SharedResources.en-US.resx
autonomous: true
requirements:
  - I18N-02

must_haves:
  truths:
    - "All static UI text (page titles, buttons, placeholders, dialog content, labels) displays in selected language"
    - "Every localized component subscribes to LanguageChanged for no-reload switching"
    - "Dynamic content (user input, log messages, module runtime output) is NOT translated"
  artifacts: []
  key_links:
    - from: "src/OpenAnima.Core/Components/Pages/Dashboard.razor"
      to: "src/OpenAnima.Core/Services/LanguageService.cs"
      via: "Subscribe to LanguageChanged for re-render"
      pattern: "LanguageChanged"
    - from: "src/OpenAnima.Core/Components/Shared/AnimaListPanel.razor"
      to: "src/OpenAnima.Core/Services/LanguageService.cs"
      via: "Subscribe to LanguageChanged for re-render"
      pattern: "LanguageChanged"
---

<objective>
Localize all remaining components with IStringLocalizer injection and LanguageChanged subscription. This is the mechanical sweep that applies the i18n infrastructure created in Plan 02 to all 11 remaining components.

Purpose: Complete I18N-02 coverage — all static UI text must display in the selected language.
Output: All components localized, .resx files updated with any newly discovered keys.

**Fallback language note:** Per CONTEXT.md locked decision, Chinese (zh-CN) is the fallback language.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-service-migration-i18n/24-RESEARCH.md
@.planning/phases/24-service-migration-i18n/24-CONTEXT.md
@.planning/phases/24-service-migration-i18n/24-02-SUMMARY.md

<interfaces>
<!-- LanguageService and IStringLocalizer are available from Plan 02 -->
<!-- _Imports.razor already has @using Microsoft.Extensions.Localization and @using OpenAnima.Core.Resources -->

Localization pattern (established in Plan 02 MainLayout):
```razor
@inject IStringLocalizer<SharedResources> L
@inject LanguageService LangSvc
@implements IDisposable

protected override void OnInitialized()
{
    LangSvc.LanguageChanged += OnLanguageChanged;
}

private void OnLanguageChanged() => InvokeAsync(StateHasChanged);

public void Dispose() => LangSvc.LanguageChanged -= OnLanguageChanged;
```

Existing .resx keys from Plan 02 (SharedResources.zh-CN.resx):
- Nav.*, Anima.*, Settings.*, Chat.*, Common.*, Dashboard.*, Heartbeat.*, Monitor.*, Modules.*, Editor.*
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Localize all remaining components with IStringLocalizer</name>
  <files>
    src/OpenAnima.Core/Components/Pages/Dashboard.razor,
    src/OpenAnima.Core/Components/Pages/Modules.razor,
    src/OpenAnima.Core/Components/Pages/Heartbeat.razor,
    src/OpenAnima.Core/Components/Pages/Monitor.razor,
    src/OpenAnima.Core/Components/Pages/Editor.razor,
    src/OpenAnima.Core/Components/Shared/AnimaListPanel.razor,
    src/OpenAnima.Core/Components/Shared/AnimaCreateDialog.razor,
    src/OpenAnima.Core/Components/Shared/AnimaContextMenu.razor,
    src/OpenAnima.Core/Components/Shared/ConfirmDialog.razor,
    src/OpenAnima.Core/Components/Shared/ChatPanel.razor,
    src/OpenAnima.Core/Components/Shared/ChatInput.razor,
    src/OpenAnima.Core/Resources/SharedResources.zh-CN.resx,
    src/OpenAnima.Core/Resources/SharedResources.en-US.resx
  </files>
  <action>
    For EACH component listed:

    1. Add `@inject IStringLocalizer<SharedResources> L` and `@inject LanguageService LangSvc`
    2. Add `@implements IDisposable` (if not already present)
    3. Replace all hardcoded Chinese/English UI text strings with `L["Key"]` references:
       - Page titles, section headers, button labels, placeholder text, tooltip text, dialog content, error messages
       - Do NOT translate: dynamic content (user input, log messages, module names from runtime, chat messages)
    4. Subscribe to `LangSvc.LanguageChanged += OnLanguageChanged;` in `OnInitialized()`
    5. Add handler: `private void OnLanguageChanged() => InvokeAsync(StateHasChanged);`
    6. Unsubscribe in `Dispose()`: `LangSvc.LanguageChanged -= OnLanguageChanged;`

    Specific component notes:
    - **Dashboard.razor**: Localize page title, status labels, card headers
    - **Modules.razor**: Localize page title, "No modules loaded" message, column headers
    - **Heartbeat.razor**: Localize page title, Start/Stop buttons, status labels (Running/Stopped), metric labels (Tick Count, Latency, Skipped)
    - **Monitor.razor**: Localize page title, section headers
    - **Editor.razor**: Localize page title, toolbar labels
    - **AnimaListPanel.razor**: Localize "Create Anima" button tooltip, empty state message
    - **AnimaCreateDialog.razor**: Localize dialog title, placeholder, Create/Cancel buttons
    - **AnimaContextMenu.razor**: Localize menu items (Rename, Clone, Delete)
    - **ConfirmDialog.razor**: Localize Confirm/Cancel buttons
    - **ChatPanel.razor**: Localize placeholder text, Send button, empty state message
    - **ChatInput.razor**: Localize placeholder text, Send button label

    Add any missing translation keys to BOTH .resx files (zh-CN and en-US) as you discover hardcoded strings.

    IMPORTANT: Do NOT translate dynamic content per user decision:
    - User chat messages
    - Log messages
    - Module runtime output
    - Module names from PluginRegistry
  </action>
  <verify>
    <automated>cd /home/user/OpenAnima && dotnet build src/OpenAnima.Core --no-restore -v minimal 2>&1 | tail -20 && echo "--- IStringLocalizer coverage check ---" && for f in src/OpenAnima.Core/Components/Pages/Dashboard.razor src/OpenAnima.Core/Components/Pages/Modules.razor src/OpenAnima.Core/Components/Pages/Heartbeat.razor src/OpenAnima.Core/Components/Pages/Monitor.razor src/OpenAnima.Core/Components/Pages/Editor.razor src/OpenAnima.Core/Components/Shared/AnimaListPanel.razor src/OpenAnima.Core/Components/Shared/AnimaCreateDialog.razor src/OpenAnima.Core/Components/Shared/AnimaContextMenu.razor src/OpenAnima.Core/Components/Shared/ConfirmDialog.razor src/OpenAnima.Core/Components/Shared/ChatPanel.razor src/OpenAnima.Core/Components/Shared/ChatInput.razor; do grep -q 'IStringLocalizer' "$f" && echo "OK: $f" || echo "MISSING: $f"; done</automated>
  </verify>
  <done>All static UI text in all 11 components uses IStringLocalizer. Every component subscribes to LanguageChanged for no-reload switching. No hardcoded UI strings remain in .razor files (except dynamic content). Project compiles without errors. All 11 targeted .razor files contain IStringLocalizer injection (verified by grep).</done>
</task>

</tasks>

<verification>
1. `dotnet build` succeeds with no errors
2. All 11 .razor components inject IStringLocalizer (grep verification passes with no MISSING lines)
3. All 11 .razor components subscribe to LanguageChanged
4. No hardcoded UI text strings remain in .razor files (except dynamic content)
5. SharedResources.zh-CN.resx and SharedResources.en-US.resx contain matching keys for all discovered strings
</verification>

<success_criteria>
- All static UI text uses IStringLocalizer across all components
- Every component subscribes to LanguageChanged for no-reload switching
- Dynamic content is NOT translated
- .resx files updated with any newly discovered keys
- Project compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/24-service-migration-i18n/24-03-SUMMARY.md`
</output>
