# Phase 66: Platform Persistence - Wave 3 (Verification)

**Plan:** 66-03
**Wave:** 3 (Verification — depends on Waves 1-2)
**Depends On:** 66-02 (integration complete)
**Files Modified:**
- `tests/OpenAnima.Tests/ChatPersistence/ChatHistoryServiceTests.cs` (new)
- `tests/OpenAnima.Tests/ViewportPersistence/ViewportStateServiceTests.cs` (new)
- `tests/OpenAnima.Tests/Services/ChatContextManagerTests.cs` (modified)
- `tests/OpenAnima.Tests/Integration/ChatPersistenceIntegrationTests.cs` (new)

**Autonomous:** true
**Requirements Addressed:** PERS-01 (verification), PERS-02 (verification), PERS-03 (verification)

---

## Objective

Verify all three persistence layers (viewport JSON, chat SQLite, token-budget truncation) work correctly through unit and integration tests.

---

## Tasks

### Task 1: Create ChatHistoryServiceTests (Store/Restore/Interrupted)

**Milestone:** Unit tests for ChatHistoryService
**Complexity:** Medium
**Owner:** Executor

<read_first>
- `src/OpenAnima.Core/ChatPersistence/ChatHistoryService.cs` — implementation to test
- `tests/OpenAnima.Tests/Unit/` — existing unit test structure and patterns
- `.planning/phases/66-platform-persistence/66-RESEARCH.md` § "Pitfall 1: Chat History Lost" — what to test for
</read_first>

<action>
1. Create file: `tests/OpenAnima.Tests/ChatPersistence/ChatHistoryServiceTests.cs`
2. Test class: `public class ChatHistoryServiceTests : IAsyncLifetime`
3. Setup: Create in-memory or temp SQLite database for each test
4. Tests to implement:
   - `StoreUserMessageAsync_InsertsRow()` — verify INSERT succeeds
   - `StoreAssistantMessageAsync_WithToolCalls()` — verify tool_calls_json is serialized
   - `LoadHistoryAsync_ReturnsMessagesInOrder()` — verify SELECT returns chronological order
   - `LoadHistoryAsync_WithInterruptedMessage_AppendsLabel()` — verify [interrupted] suffix added
   - `LoadHistoryAsync_FiltersBy AnimaId()` — verify different Animas have separate histories
   - `StoreMessage_WithNullToolCalls_Succeeds()` — verify null handling
5. Each test should:
   - Create fresh database for isolation
   - Call appropriate service methods
   - Assert database state or return values
   - Clean up (database deleted on dispose)
6. Verify: All 6 tests pass, ~100% coverage of ChatHistoryService methods
</action>

<acceptance_criteria>
- [ ] File `tests/OpenAnima.Tests/ChatPersistence/ChatHistoryServiceTests.cs` exists
- [ ] Contains test class `ChatHistoryServiceTests`
- [ ] Contains test: `StoreUserMessageAsync_InsertsRow` — verifies message persisted
- [ ] Contains test: `LoadHistoryAsync_ReturnsMessagesInOrder` — verifies chronological order (ORDER BY created_at ASC)
- [ ] Contains test: `LoadHistoryAsync_WithInterruptedMessage_AppendsLabel` — verifies [interrupted] suffix
- [ ] Contains test: `LoadHistoryAsync_FiltersBy AnimaId` — verifies per-Anima isolation
- [ ] All tests use isolated SQLite database
- [ ] Tests pass: `dotnet test tests/OpenAnima.Tests/ChatPersistence/ChatHistoryServiceTests.cs` exits 0
</acceptance_criteria>

---

### Task 2: Create ViewportStateServiceTests (Save/Load/Debounce)

**Milestone:** Unit tests for ViewportStateService
**Complexity:** Medium
**Owner:** Executor

<read_first>
- `src/OpenAnima.Core/ViewportPersistence/ViewportStateService.cs` — implementation to test
- `tests/OpenAnima.Tests/Unit/` — existing test patterns
- `.planning/phases/66-platform-persistence/66-RESEARCH.md` § "Pitfall 2: Viewport State Not Restored" — what to test for
</read_first>

<action>
1. Create file: `tests/OpenAnima.Tests/ViewportPersistence/ViewportStateServiceTests.cs`
2. Test class: `public class ViewportStateServiceTests : IAsyncLifetime`
3. Setup: Create temp directory for each test
4. Tests to implement:
   - `LoadAsync_FileDoesNotExist_ReturnsDefault()` — verify default ViewportState (1.0, 0, 0)
   - `LoadAsync_FileExists_ReturnsDeserialized()` — verify JSON deserialization
   - `TriggerSaveViewport_WritesJsonFile()` — verify file is created
   - `TriggerSaveViewport_WithDebounce_OnlyOneWrite()` — verify 1000ms debounce (call multiple times, only one file write)
   - `TriggerSaveViewport_CancelBeforeDelay_FileNotCreated()` — verify cancellation works
   - `LoadAsync_InvalidJson_ReturnsDefault()` — verify error handling returns default
5. Each test should:
   - Use temp directory for file isolation
   - Call appropriate service methods
   - Assert file state or return values
   - Clean up temp files on dispose
6. Debounce test must wait and verify timing:
   - Call TriggerSaveViewport 3 times rapidly
   - Wait 1100ms
   - Verify only 1 file write occurred (check file modification times)
7. Verify: All 6 tests pass
</action>

<acceptance_criteria>
- [ ] File `tests/OpenAnima.Tests/ViewportPersistence/ViewportStateServiceTests.cs` exists
- [ ] Contains test class `ViewportStateServiceTests`
- [ ] Contains test: `LoadAsync_FileDoesNotExist_ReturnsDefault` — verifies scale=1.0, panX=0, panY=0
- [ ] Contains test: `TriggerSaveViewport_WritesJsonFile` — verifies file created
- [ ] Contains test: `TriggerSaveViewport_WithDebounce_OnlyOneWrite` — verifies 1000ms debounce works
- [ ] Contains test: `LoadAsync_InvalidJson_ReturnsDefault` — verifies error handling
- [ ] All tests use isolated temp directory
- [ ] Tests pass: `dotnet test tests/OpenAnima.Tests/ViewportPersistence/ViewportStateServiceTests.cs` exits 0
</acceptance_criteria>

---

### Task 3: Add ChatContextManager Token Truncation Tests

**Milestone:** Unit tests for context truncation
**Complexity:** Medium
**Owner:** Executor

<read_first>
- `src/OpenAnima.Core/Services/ChatContextManager.cs` — TruncateHistoryToContextBudget method
- `tests/OpenAnima.Tests/Services/ChatContextManagerTests.cs` — existing test file (may need to be created)
- `.planning/phases/66-platform-persistence/66-RESEARCH.md` § "Pitfall 3: Token Budget Not Respected" — what to test for
</read_first>

<action>
1. Open or create `tests/OpenAnima.Tests/Services/ChatContextManagerTests.cs`
2. Add tests for token truncation:
   - `TruncateHistoryToContextBudget_WithinBudget_ReturnsAll()` — 5 messages, 1000 token budget, all returned
   - `TruncateHistoryToContextBudget_OverBudget_ReturnsRecent()` — 50 messages, 2000 token budget, returns only last N
   - `TruncateHistoryToContextBudget_MaintainsChronologicalOrder()` — returned messages are oldest-first
   - `TruncateHistoryToContextBudget_EmptyList_ReturnsEmpty()` — edge case
   - `TruncateHistoryToContextBudget_SingleMessage_ReturnsIt()` — edge case
3. Each test:
   - Creates ChatSessionMessage list with known token counts (mock or hardcoded)
   - Calls TruncateHistoryToContextBudget with specific budget
   - Asserts returned list meets criteria
4. Verify: All tests pass, token counting is accurate
</action>

<acceptance_criteria>
- [ ] File `tests/OpenAnima.Tests/Services/ChatContextManagerTests.cs` exists or is modified
- [ ] Contains test: `TruncateHistoryToContextBudget_WithinBudget_ReturnsAll`
- [ ] Contains test: `TruncateHistoryToContextBudget_OverBudget_ReturnsRecent` — verifies recent messages kept
- [ ] Contains test: `TruncateHistoryToContextBudget_MaintainsChronologicalOrder` — verifies oldest-first order
- [ ] Tests use realistic token counts (e.g., ~200 tokens per typical message)
- [ ] Tests pass: `dotnet test tests/OpenAnima.Tests/Services/ChatContextManagerTests.cs -k "Truncate" -x` exits 0
</acceptance_criteria>

---

### Task 4: Create ChatPersistenceIntegrationTests (E2E Lifecycle)

**Milestone:** Integration tests for full chat persistence flow
**Complexity:** High
**Owner:** Executor

<read_first>
- `tests/OpenAnima.Tests/Integration/` — existing integration test structure
- `src/OpenAnima.Core/ChatPersistence/ChatHistoryService.cs` and ChatDbInitializer — implementations under test
- `.planning/phases/66-platform-persistence/66-RESEARCH.md` § "Pattern 4: On-Startup Integration" — full lifecycle flow
</read_first>

<action>
1. Create file: `tests/OpenAnima.Tests/Integration/ChatPersistenceIntegrationTests.cs`
2. Test class: `public class ChatPersistenceIntegrationTests : IAsyncLifetime`
3. Setup: Create test database, initialize schema, inject services
4. Integration tests:
   - `FullChatLifecycle_StoreAndRestore()` — store 5 messages, load, verify all restored
   - `MultiAnimaIsolation_DifferentAnimasHaveSeparateHistories()` — store for anima-1 and anima-2, load each, verify isolation
   - `InterruptedMessageHandling_RestoresWithLabel()` — store message with IsStreaming=true, load, verify [interrupted] suffix
   - `TokenBudgetTruncation_TruncatesHistoryCorrectly()` — store 50 messages, truncate to 3000 tokens, verify ~15 messages returned
   - `PersistenceAcrossRestart_SimulatesAppShutdown()` — close database, reopen, verify data intact
5. Each test:
   - Uses real SQLite database (in temp directory)
   - Calls full service lifecycle (init, store, load)
   - Asserts data integrity across operations
   - Cleans up temp files on dispose
6. Verify: All integration tests pass, full lifecycle works end-to-end
</action>

<acceptance_criteria>
- [ ] File `tests/OpenAnima.Tests/Integration/ChatPersistenceIntegrationTests.cs` exists
- [ ] Contains test: `FullChatLifecycle_StoreAndRestore` — verifies data survives store + load cycle
- [ ] Contains test: `MultiAnimaIsolation_DifferentAnimasHaveSeparateHistories` — verifies anima_id filtering
- [ ] Contains test: `InterruptedMessageHandling_RestoresWithLabel` — verifies [interrupted] suffix on restore
- [ ] Contains test: `TokenBudgetTruncation_TruncatesHistoryCorrectly` — verifies context truncation works
- [ ] Contains test: `PersistenceAcrossRestart_SimulatesAppShutdown` — verifies data survives database close/reopen
- [ ] Tests use real SQLite database (not mocked)
- [ ] Tests pass: `dotnet test tests/OpenAnima.Tests/Integration/ChatPersistenceIntegrationTests.cs` exits 0
</acceptance_criteria>

---

### Task 5: Create ViewportPersistenceIntegrationTests

**Milestone:** Integration tests for viewport persistence
**Complexity:** Medium-High
**Owner:** Executor

<read_first>
- `tests/OpenAnima.Tests/Integration/` — existing integration test patterns
- `src/OpenAnima.Core/ViewportPersistence/ViewportStateService.cs` — implementation under test
- `.planning/phases/66-platform-persistence/66-RESEARCH.md` § "Example 3: Viewport Restore" — full lifecycle
</read_first>

<action>
1. Create file: `tests/OpenAnima.Tests/Integration/ViewportPersistenceIntegrationTests.cs`
2. Test class: `public class ViewportPersistenceIntegrationTests : IAsyncLifetime`
3. Setup: Create temp config directory, inject service
4. Integration tests:
   - `FullViewportLifecycle_SaveAndRestore()` — save viewport, load, verify exact values
   - `MultiAnimaViewports_EachAnimaHasOwnViewportFile()` — save for 3 different Animas, load each, verify separate files
   - `ViewportDebounce_RapidChangesProduceSingleFile()` — trigger save 10 times rapidly, verify only 1 file in directory
   - `ViewportPersistenceAcrossRestart()` — save, close service, reopen, verify values restored
   - `ViewportDefaultValues_FileDoesNotExist_ReturnsDefaults()` — load non-existent Anima, verify scale=1.0, pan=(0,0)
5. Each test:
   - Uses real filesystem (temp directory)
   - Calls full service methods (save, load)
   - Asserts file state and loaded values
   - Cleans up temp files on dispose
6. Verify: All viewport tests pass, file I/O works reliably
</action>

<acceptance_criteria>
- [ ] File `tests/OpenAnima.Tests/Integration/ViewportPersistenceIntegrationTests.cs` exists
- [ ] Contains test: `FullViewportLifecycle_SaveAndRestore` — verifies exact value round-trip
- [ ] Contains test: `MultiAnimaViewports_EachAnimaHasOwnViewportFile` — verifies {animaId}.viewport.json naming
- [ ] Contains test: `ViewportDebounce_RapidChangesProduceSingleFile` — verifies debounce reduces writes
- [ ] Contains test: `ViewportPersistenceAcrossRestart` — verifies data survives service restart
- [ ] Tests use real filesystem (not mocked)
- [ ] Tests pass: `dotnet test tests/OpenAnima.Tests/Integration/ViewportPersistenceIntegrationTests.cs` exits 0
</acceptance_criteria>

---

### Task 6: Run Full Test Suite and Verify Coverage

**Milestone:** Verify all tests pass and coverage is adequate
**Complexity:** Low
**Owner:** Executor

<read_first>
- `tests/OpenAnima.Tests/` — full test directory
- `.planning/phases/66-platform-persistence/66-RESEARCH.md` § "Phase Requirements → Test Map" — required tests
</read_first>

<action>
1. Run chat persistence tests: `dotnet test tests/OpenAnima.Tests/ChatPersistence/ -x`
   - Verify all ChatHistoryServiceTests pass
2. Run viewport persistence tests: `dotnet test tests/OpenAnima.Tests/ViewportPersistence/ -x`
   - Verify all ViewportStateServiceTests pass
3. Run ChatContextManager token tests: `dotnet test tests/OpenAnima.Tests/Services/ChatContextManagerTests.cs -k "Truncate" -x`
   - Verify all truncation tests pass
4. Run integration tests: `dotnet test tests/OpenAnima.Tests/Integration/ChatPersistenceIntegrationTests.cs tests/OpenAnima.Tests/Integration/ViewportPersistenceIntegrationTests.cs -x`
   - Verify all E2E tests pass
5. Run full suite: `dotnet test tests/OpenAnima.Tests/ --no-build`
   - Verify no regressions, all 662+ tests pass
6. Verify coverage:
   - ChatHistoryService: 100% (all public methods tested)
   - ViewportStateService: 100% (all public methods tested)
   - ChatContextManager.TruncateHistoryToContextBudget: 100% (all paths tested)
</action>

<acceptance_criteria>
- [ ] ChatHistoryServiceTests: all pass (`dotnet test tests/OpenAnima.Tests/ChatPersistence/ChatHistoryServiceTests.cs -x` exits 0)
- [ ] ViewportStateServiceTests: all pass (`dotnet test tests/OpenAnima.Tests/ViewportPersistence/ViewportStateServiceTests.cs -x` exits 0)
- [ ] ChatContextManager truncation tests: all pass (`dotnet test tests/OpenAnima.Tests/Services/ChatContextManagerTests.cs -k "Truncate" -x` exits 0)
- [ ] ChatPersistenceIntegrationTests: all pass (`dotnet test tests/OpenAnima.Tests/Integration/ChatPersistenceIntegrationTests.cs -x` exits 0)
- [ ] ViewportPersistenceIntegrationTests: all pass (`dotnet test tests/OpenAnima.Tests/Integration/ViewportPersistenceIntegrationTests.cs -x` exits 0)
- [ ] Full test suite passes: `dotnet test tests/OpenAnima.Tests/ --no-build` exits 0
- [ ] No test regressions (all existing tests still pass)
</acceptance_criteria>

---

### Task 7: Manual Verification — Full Persistence Workflow

**Milestone:** Verify all requirements work in actual running application
**Complexity:** Medium
**Owner:** QA/Manual Testing

<read_first>
- `.planning/phases/66-platform-persistence/66-CONTEXT.md` — locked decisions to verify
- `.planning/REQUIREMENTS.md` § "PERS-01, PERS-02, PERS-03" — requirements checklist
</read_first>

<action>
1. Build and run application: `dotnet run` in src/OpenAnima.Core
2. **PERS-01 Verification (Viewport Persistence):**
   - Navigate to editor for an Anima
   - Pan editor to position (e.g., panX=200, panY=150)
   - Zoom to scale 1.5
   - Close application completely
   - Restart application
   - Navigate back to same Anima
   - **Verify:** Editor is at pan (200, 150) and scale 1.5 (exact same position)
   - **Also verify:** `{animaId}.viewport.json` file exists in data directory
3. **PERS-02 Verification (Chat History):**
   - Open chat for an Anima
   - Send 5 messages (mix of user and assistant)
   - Close application
   - Restart application
   - Navigate back to same Anima's chat
   - **Verify:** All 5 messages appear in correct order
   - **Verify:** No messages marked [incomplete] (app shut down gracefully)
   - **Also verify:** `chat.db` file exists in data directory with chat_messages table
4. **PERS-03 Verification (Token Budget):**
   - In Settings, set LLM Context Token Budget to 1000
   - Send 20 messages (each ~200 tokens)
   - Look at app logs or add debug output to confirm LLM received only ~5 messages (1000 token budget)
   - **Verify:** Full history (20 messages) shown in UI scrollback
   - **Verify:** Only recent messages (~5) sent to LLM
5. **Multi-Anima Isolation:**
   - Create/switch to a different Anima
   - Pan editor differently
   - Send different messages
   - Switch back to first Anima
   - **Verify:** First Anima's viewport and chat are restored exactly
   - Switch to second Anima
   - **Verify:** Second Anima's viewport and chat match what was entered
6. **Interrupted Message Handling:**
   - Send a long-running message (or manually kill app during streaming)
   - Restart app
   - **Verify:** Message appears with [interrupted] suffix or label
</action>

<acceptance_criteria>
- [ ] PERS-01: Editor viewport restored to exact position after restart
- [ ] PERS-01: {animaId}.viewport.json file exists in data directory
- [ ] PERS-02: All chat messages restored in correct order after restart
- [ ] PERS-02: chat.db file exists in data directory
- [ ] PERS-02: No spurious [interrupted] labels on graceful shutdown
- [ ] PERS-03: Settings allows configuring token budget (1000-128000)
- [ ] PERS-03: Full chat history shown in UI (not truncated visually)
- [ ] PERS-03: LLM context limited to token budget (verified in logs or behavior)
- [ ] Multi-Anima: Each Anima's viewport and chat are separate and restored correctly
- [ ] Interrupted messages: [interrupted] suffix visible when app crashes mid-stream
</acceptance_criteria>

---

## Phase Verification

**Requirements Coverage:**
- PERS-01: ✓ Viewport JSON storage + restore + debounce (Wave 1-2, verified Wave 3)
- PERS-02: ✓ Chat SQLite storage + restore + interrupted flag (Wave 1-2, verified Wave 3)
- PERS-03: ✓ Token-budget-based truncation + Settings UI (Wave 2, verified Wave 3)

**Success Criteria:**
1. All unit tests pass (ChatHistoryService, ViewportStateService, ChatContextManager)
2. All integration tests pass (chat lifecycle, viewport lifecycle, multi-anima)
3. Full test suite passes (662+ tests, no regressions)
4. Manual verification passes (viewport, chat, token budget, multi-anima, interrupted handling)
5. Phase requirements fully met

**Exit Criteria:**
- [ ] All Wave 3 tasks completed
- [ ] All tests green (unit + integration + full suite)
- [ ] Manual verification passed
- [ ] Code compiles without warnings
- [ ] Commit: "feat(66): platform persistence complete"
